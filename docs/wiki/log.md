# Wiki log

Append-only, chronological. One line per entry, newest at the bottom.
Format: `YYYY-MM-DD TAG — note`. Tags: `INGEST` (new/updated knowledge),
`LINT` (health check), `DECISION` (a choice + its why), `FIX` (bug learning).

- 2026-07-08 INGEST — Wiki created (Karpathy LLM-wiki pattern). Seeded architecture, conventions, gotchas, widget-and-embed, bot-config, plans-and-entitlements, languages-i18n, rag-and-knowledge, commerce, access-model, blog-and-seo, voice from this session + prior working notes. rag/commerce/voice marked ⚠️ verify.
- 2026-07-08 FIX — Widget "currently unavailable": embed iframe's same-origin config fetch (no Origin header) was rejected by a non-empty allowlist; first-party requests now bypass it (`lib/widget-auth.ts`). See gotchas.
- 2026-07-08 DECISION — Free tier = one language of the client's choice (not English-only). `maxLanguages` entitlement; content de-anchored from `content.en`. See languages-i18n.
- 2026-07-08 FIX — Voice agent replies were samey ("Žinoma!" + a canned line) because `buildAgentPrompt` gave that exact phrase as the product-reply example, which gpt-4o parroted. Rewrote the delivery + product guidance to demand varied, request-specific, consultative phrasing; bumped `agentConfigHash` marker to `v21` so agents re-sync. Gotcha captured in voice.md: the hash doesn't cover hardcoded voice-block text.
- 2026-07-08 FIX — Voice agent leaked a Russian word ("обрадует") into a Lithuanian reply (gpt-4o code-switching). Added a language-lock to `buildAgentPrompt` keyed to the bot's `languages[]` (reply only in available languages, never Russian/others, no mid-reply mixing, don't switch unless the customer does). Bumped hash to `v22`. ElevenLabs V3 auto-detect is an unconstrained separate layer — next lever if it recurs.
- 2026-07-08 FIX — Voice: one question produced 3 product carousels + a loosely-related jumble. Cause: each `search_products` voice call renders its own list (`ChatWindow.handleVoiceSearch`) and the gift prompt told the agent to search several categories. Fixed: voice prompt now runs ONE focused search per request + asks a clarifying question (hash `v23`); `handleVoiceSearch` merges a burst of searches into one list. Pre-existing setState-in-effect eslint errors in ChatWindow left untouched (out of scope).
- 2026-07-10 INGEST — 3 AEO/SEO blog posts added (voice how-to `add-voice-ai-to-online-store`, WISMO `where-is-my-order-ai`, `ai-customer-service-small-stores`), each with 2 generated images + added to llms.txt. Voice-led angle mirrors the new hero. Gotcha: a blog title starting with `"` must be single-quote-wrapped in frontmatter (`title: '"..."'`) — `parseFrontmatter` strips one leading + one trailing quote.
- 2026-07-09 INGEST — Guided resolution for knowledge-check findings: "Resolve" opens `LintResolveDialog` (pick the correct line / keep both) → `POST /api/knowledge/lint/resolve` applies model-proposed verbatim find/replace edits to the source(s), re-ingests, dismisses. Also: lint prompt is now date-aware (no more "2026 is future" false positive); SourceDrawer shows a markdown preview + Edit toggle.
- 2026-07-08 INGEST — Knowledge-check findings are now resolvable: each links to its source(s) + an AI `suggestedFix`; "Fix" opens the source editor, "Dismiss" persists to `knowledge_lint_dismissals` (migration 0038). `lint.ts` maps excerpt refs→sources + adds stable fingerprints; lint route tolerates the table being absent (safe to deploy pre-migration).
- 2026-07-08 LINT — Hardened rag-and-knowledge, commerce, voice against the code (removed ⚠️ verify seeds). Corrections worth knowing: in-chat TTS/STT are RETIRED — `publicBotConfig` hardcodes them off, only the live-call agent works (`lib/widget-config.ts:199-205`); order lookup works only for woocommerce + magento, and the chat "discount" is a static configured code, not a live coupon API (`lib/commerce/index.ts`); the `api/widget/{order,search,discount}` routes back the VOICE agent, not the text chat tools; RAG has a canonical-pages subsystem (`lib/ingestion/canonical.ts`) the notes missed. Residual ⚠️ verify: voice per-minute metering (none in-repo), RAG eval "10/10" is point-in-time.
- 2026-07-10 INGEST — Added opt-in proactive launcher greetings: per-language random variants, immediate/delayed display, once-per-session/every-page frequency, custom colors/font/radius, dismiss/open interactions, and a faithful deterministic configurator preview. The host-page loader keeps the chat iframe lazy and respects reduced motion. See bot-config and widget-and-embed.
- 2026-07-10 DECISION — Reorganized Appearance around progressive disclosure and visitor-facing tasks. Core colors are compact cards; hex editing moved into a color dialog; advanced colors are hidden until requested; shapes use dense scrubbers; launcher, greeting, and chat-surface settings are separate groups. See bot-config.
- 2026-07-10 DECISION — Extended the Appearance hierarchy across the configurator: every feature section now uses compact task-based cards, conditional details stay disclosed only when enabled, and mixed select/slider grids use external field titles for consistent alignment. See bot-config.
- 2026-07-10 DECISION — Refined configurator density: secondary field explanations moved to accessible info tooltips, language availability became a multi-select dropdown distinct from primary language/content editing, compact sliders now share select geometry, and expanded section surfaces match their headers. See bot-config.
- 2026-07-10 DECISION — Config sections now form a gapless stack with white, left-gradient headers over muted expanded bodies. Save became “Save & Publish” and remains disabled until config or bot-name state is dirty; successful publishing resets that baseline. See bot-config.
- 2026-07-10 DECISION — Section headers now own a single bottom divider and show their animated left gradient only while expanded (with reduced-motion fallback); collapsed headers stay plain white. Shortened Store and Allowed domains summaries and separated the two language selectors by 20px.
- 2026-07-10 DECISION — Color editing now keeps the floating widget preview sharp above the dialog's blurred backdrop and streams valid picker/hex changes live. Cancelling or dismissing restores the opening color; Apply commits it. Other dialogs retain normal full-screen modality.
- 2026-07-10 FIX — The first color-preview layering attempt still blurred the widget because its z-index remained trapped inside the configurator preview pane. The live widget now renders through a body-level portal, with explicit color-dialog layers (backdrop 40 → preview 45 → content 50).
- 2026-07-10 FIX — Live proactive greeting reused the launcher's 24px/2.5px-stroke close SVG, making its X larger than the preview's Lucide icon. Added a dedicated 16px/2px-stroke greeting X and a loader regression assertion.
- 2026-07-10 FIX — Landing-page widget teardown omitted the body-mounted proactive greeting, leaving it visible after navigation/login while the launcher disappeared. `WidgetEmbed` now removes the greeting with every other widget surface and has a lifecycle regression test.
- 2026-07-10 INGEST — Added `docs/CHANNELS_IMPLEMENTATION.md`: Messenger-first scope, subscription/connection UX, Meta setup, provider architecture, schema, billing, webhook, security, Inbox, testing, rollout, and definition of done.
- 2026-07-10 FIX — The widget loader's “Powered by Loqara” badge still targeted the retired `loqara.app` domain. It now uses the canonical `https://www.loqara.com` marketing URL, protected by a loader regression test.
- 2026-07-11 DECISION — Sales leads became a responsive CRM-style workspace with summary metrics, compact filters, mobile lead cards, and an accessible detail panel. Clipboard copy now contains only `email_body`; stored outreach emphasizes contextual Lithuanian conversation and the verified 1,500–12,000 paid-plan limits. See sales-leads.
- 2026-07-11 DECISION — Refined the sales-leads workspace into the owner UI's compact page hierarchy and a flatter score-first data grid. Platform is a dedicated column, city is detail-only, and status colors now encode the lead lifecycle. See sales-leads.
- 2026-07-11 FIX — Sales-lead details no longer collapse long cards or clip the prepared email: the drawer now has a flex-constrained scroll viewport, non-shrinking content sections, clickable website metadata, and an isolated Base UI starting/ending-state slide transition. See sales-leads.
- 2026-07-11 DECISION — LinkedIn planning is now a responsive, keyboard-accessible draggable board with persisted per-column order, 20 founder-led drafts, 10 distinct ideas, and 20 exact-text editorial visuals. Stable dnd-kit context IDs prevent SSR hydration mismatches. See linkedin-content.
- 2026-07-11 FIX — Owner dashboard LEADS card showed 5: not a code bug, just seeded Aurora Living demo leads (identical timestamps, fake names). Deleted the 5 demo rows; card now reads real 0. See owner-dashboard.
- 2026-07-11 FEAT — Added an MRR/earnings headline card to the owner dashboard (`MrrCard` + `computeMrr`, DB-only, no Stripe call). Live snapshot only — no MRR history stored yet, so a growth-over-time chart is deferred. See owner-dashboard.
- 2026-07-11 CHORE — Seed script (`scripts/seed-linkedin-content.mjs`) now defines the draft board order via an explicit `DRAFT_ORDER` id list (+ coverage guard) instead of array position, so a reseed restores the recommended founder-POV-first sequence. See linkedin-content.
- 2026-07-11 DECISION — Lint cleared to 0 errors: fixed 3 mechanical issues (unescaped entity, 2× prefer-const) and downgraded the 5 new React-Compiler `react-hooks` rules to `warn` (they fire on intentional, hydration-safe pre-compiler patterns). See gotchas.
- 2026-07-11 FEAT — Added per-draft LinkedIn hashtags (3–5 each) in `scripts/linkedin-hashtags.mjs`, appended to the 20 draft bodies for discoverability. Shared by the seed (build-time append, tag-free source so no double-append) and a one-off DB backfill. See linkedin-content.
- 2026-07-11 FEAT — Commerce agent is now aware of its own suggestion cards across turns: last shown products are read back from `messages.products`, injected into the prompt (numbered, display order) and seeded into a separate `shown` map so `display_products` can re-show by id. Verified end-to-end ("tell me about the first one" resolves; no phantom cards on non-product turns). See commerce.
- 2026-07-11 FEAT — Commerce agent can now discuss and compare products: semantic matches carry the indexed doc (categories/tags/attributes/desc, title-stripped, ≤400 chars) as `CommerceProduct.details`, returned to the model for the top 8 results per search. Verified end-to-end (meaningful two-candle comparison with scent-specific reasoning). See commerce.
- 2026-07-11 FEAT — `get_product_details` tool (WooCommerce only): live full description (≤1500 chars) + attribute lines for up to 3 ids via the public Store API, behind the SSRF guard. Also fixed the shown-cards prompt block to include ids — without them the model guessed ids cross-turn and details/re-display silently failed. Verified end-to-end (burn time, wax blend, fragrance notes answered from live data). See commerce.
- 2026-07-11 FEAT — Product details now work on Shopify (Storefront `nodes` query: full description + options-as-attributes) and on the VOICE agent for both Woo+Shopify: new `get_product_details` client tool (expects_response, name-based — voice LLM never sees ids) → `/api/widget/details` resolves the spoken name via searchCatalog and returns a spoken summary. Agent hash bumped to v24 so existing voice agents re-sync. See commerce.
- 2026-07-12 FIX — "Slim Lady" search returned soap dispensers; three root causes fixed: (1) migration `20260712060000` adds a diacritic-folded title-token boost to `match_products` (FTS 'simple' never matched "skaidulu" vs "skaidulų"); (2) prompts/tool descriptions now force verbatim brand-name searches; (3) `audience=unisex` is neutralized to no-filter in `searchCatalog` — the RPC filter was excluding women-tagged products when the model volunteered unisex. Also: chat route now logs each model search query; supabase CLI upgraded (brew, also bumped node 22→26 — tests fine) and migration history repaired (0018–0038 marked applied). See commerce.
- 2026-07-12 FIX — Card awareness now works in the configurator PREVIEW too: it's a separate stateless route (`/api/preview/chat`) that never got the /api/chat features — buildHistory dropped card-only messages entirely and sent no products. Client now carries assistant products in preview history; route derives shownProducts + shown map. Verified with an authed request (magic-link session, revoked after). Also clarified the "Smart product search" config copy (catalog index ≠ Knowledge screen). See commerce.
- 2026-07-12 FEAT — Smart product search UX: idle view shows "X / Y products indexed · last synced … · auto-resyncs nightly" (new GET /api/products/sync status endpoint), sync progress now has live numbers for every phase (fetching count, embedding batches), copy explains the nightly cron and the catalog-vs-Knowledge split. Found via the new display: a transient page failure silently truncated the index (1,600/2,582) — fetchWooCatalog now retries a failed page once. See commerce.
- 2026-07-12 FIX — Sync 504 post-mortem: the timed-out run WIPED the index down to 400/2,582 products because refresh was delete-then-insert; now upsert-then-prune (synced_at stamp as prune key) so timeouts can't destroy the index. Index repaired via localhost run. See gotchas.
- 2026-07-12 FIX — Lead capture "After N messages" was a dead option: the enum/UI existed but nothing counted messages (only on_fallback + manual worked). Now counted client-side in ChatWindow (visitor's Nth message opens the form); `afterNMessages` threaded through publicBotConfig (it was stripped). Enabling lead capture with zero fields now seeds Name + Email defaults — an empty fields list meant the form could never render.
- 2026-07-12 FEAT — Lead form is now localized + customizable: built-in EN/LT strings (title, Send, thanks, validation) follow the widget language, with an optional per-bot `leadCapture.title` override threaded through publicBotConfig. Trigger dropdown explains each option (fallback / Nth visitor message / manual quick action) with a persistent hint; enabling lead capture seeds Name+Email field defaults in the bot's primary language (Vardas / El. paštas for LT).
- 2026-07-12 FEAT — Incremental catalog sync: `product_embeddings.raw_hash` (migration 20260712190000) + diff in sync.ts — unchanged products skip AI enrichment and embedding, just a synced_at bump. Measured: 3IMIS (coffeeloft.lt, 1,480 products) first sync ~5 min, re-sync 59 s → re-syncs fit the Vercel 300 s budget. Also fixed the progress-poll race (stale 'done'/'error' row killed the display on every retry). 3IMIS indexed 0→1,480; second HomeByNB bot re-synced. See gotchas.
- 2026-07-13 FEAT — Launcher customization: 6 selectable icons (chat/bubble/dots/help/sparkles/headset) + close-icon choice (X / arrow-down) + bottom/side spacing sliders (8–120px, side follows position). SVGs live in `lib/launcher-icons.ts` with a synced copy in `public/widget.js` (static file, can't import — a unit test asserts every key exists in both). Offsets apply to the launcher, pulse rings, greeting bubble, and panel; verified live on the landing widget (sparkles + chevron + 48/72 offsets, then reverted).
- 2026-07-13 FEAT — Launcher icon set grown to 10 (added paper plane, lightning, smiley, shopping bag); spacing sliders tightened to 10–50px. The "icons invisible" report reproduced as a deploy/compile artifact — a dev probe page with the exact picker markup rendered all icons correctly (jsdom + real browser), so the picker code is sound; hard refresh after deploy.
- 2026-07-13 FIX+FEAT — The "invisible" launcher icon picker was white-on-white: the bot's launcher color is white and the picker hardcoded white icons (the live widget auto-contrasts; the picker didn't). Picker + TestChat preview now use `readableTextColor`, and a new `theme.launcherIconColor` lets owners set an explicit color for BOTH icons (empty = auto contrast) — threaded through publicBotConfig into widget.js.
- 2026-07-13 FIX — Voice agent finally "sees" products: `search_products` (and order/discount) had ElevenLabs-default `expects_response: false`, so the LLM never received the search summary — cards rendered but the agent didn't know what it found (couldn't name products, answer "the first one", or chain into get_product_details). All client tools now set `expects_response: true`; hash bumped v26 and all four voice agents force-resynced + verified via the ElevenLabs API. Also: launcher icon swaps (paper plane→robot, question→smiley bubble).
- 2026-07-13 FIX — "Deja, neturiu išsamesnės informacijos" while the store page had a full description: the model judged the 400-char search `details` as all it had and never called get_product_details (its description was too soft — "use when search details don't cover it"). Now a hard rule in the tool description + a PRODUCT DETAILS prompt block (chat, gated on productDetailsSupported) + the voice prompt: MUST call before answering "tell me more", NEVER claim lack of info without having called it. Voice hash v27, agents resynced. E2E: TIRTIR "papasakok daugiau apie pirmą" → full ingredient breakdown.
- 2026-07-13 FIX — Voice details in the configurator preview: the preview transport's get_product_details was a stub ("not available in the preview"), so playground voice calls apologized while the live widget worked. New authed /api/preview/details mirrors /api/widget/details (name → searchCatalog top-1 → full description); transport wired. Same preview-is-a-separate-route trap as card awareness — see commerce.
- 2026-07-13 FIX — Sales leads: copyable email/phone in the detail panel + a subject copy button (subject pattern "Company: idėja svetainei" kept — short, personalized, non-salesy). Audited all 231 prepared emails with a strict LLM pass: 18 openings were unfinished fragments ("Jūsų salonai Kaune ir Klaipėdoje plius e-parduotuvė."), minimally repaired into complete sentences keeping every personalized fact, business-specific connectors, no stock phrases (dry-run reviewed before applying).
- 2026-07-13 GOTCHA — dropslietuva.com "Test connection" fails only from production: Cloudflare blocks Vercel egress at the IP/ASN level (localhost + curl pass; browser-like headers don't help — added STOREFRONT_HEADERS to all Woo storefront fetches anyway for the header-scoring case). Client-side fix: WAF exception for /wp-json/wc/store/*. See commerce.
- 2026-07-13 FEAT — Demo bots in the owner portal: /owner/demos lists/creates showcase bots in a find-or-create "Loqara Demos" org (organizations.is_demo, migration 20260713120000; enterprise+voice so the editor exposes everything). Editing reuses the done-for-you configure route; /present/[botId] is a clean full-screen stage (bg-dots + real WidgetEmbed) for screen-sharing pitches. owner_stats now excludes platform AND demo orgs (CLIENTS 7→6, conversations 104→95 — platform-bot traffic no longer counted); dashboard recent-activity + clients list filter both.
- 2026-07-13 FEAT — Demos section grew a full editor + duplication: /owner/demos/[botId]/{configure,knowledge,embed} mirror the done-for-you editor (OwnerBotTabs gained a `base` prop; violet "Editing demo" banner) so editing a demo never routes through /owner/clients. "Create from demo" on the client detail page duplicates a demo into the client org via lib/bots/duplicate.ts — config + knowledge sources/chunks (embeddings, ids remapped) + the whole product index (skips the first sync); source must be in the is_demo org, plan bot-limit enforced; verified against the real DB. Aurora Living demo account deleted (scripts/demo-cleanup.mjs) — stats now 5 clients / 52 conversations. vitest now stubs `server-only`.
- 2026-07-13 FEAT — Demos screen redesigned: brand-glow cards with avatar/monogram, store host, indexed-products badge ("605 products indexed" vs "Catalog not synced"), created date; polished New-demo panel. A permanent collapsible "Store blocks the connection? (Cloudflare)" reference embeds the client-facing WAF skip-rule instructions (URI starts with /wp-json/wc/store/ → Skip Bot Fight Mode) + the call script, so the owner never needs to re-derive it.
- 2026-07-13 FIX — Cloudflare challenge pages were being INDEXED as knowledge (dropslietuva crawl: 18/18 sources were "Performing security verification" interstitials marked Ready). `looksLikeBotChallenge` in parse.ts rejects challenge output from Jina (falls back to direct fetch, which passes from residential IPs) and errors the source when direct fetch is challenged too. Junk sources deleted; re-parse of the same URL returns real content. Also: Toaster pinned to max z-index so toasts render above the widget preview/launcher.
- 2026-07-13 FIX — Toasts genuinely above the widget preview now: the preview PORTALS to document.body (z-45) while the Toaster sat inside the layouts' `isolate` stacking context — no z-index could cross that boundary; Toaster moved outside the isolate wrapper in both owner and client layouts. Voice ASR LT→LV drift mitigated via `asr.keywords` biasing (no hard pin exists — asr.language is silently dropped); agents resynced at v28. See voice.
- 2026-07-15 FEAT — Header styles + header toggles: `theme.headerStyle` ('classic' | 'floating' inset pill | 'curved' body-card overlap), `theme.hideHeaderLogo`, `theme.compactCallButton` (icon-only square call button via VoiceCallButton's new `iconOnly` prop). Picker ("Choose a style") sits above the theme presets in Appearance; toggles live in Chat surface. New theme keys must be added in FIVE places or types break: lib/types.ts, lib/widget-config.ts (type + publicBotConfig), lib/validation/schemas.ts (field + default literal), lib/widget-theme-presets.ts (`base` + PRESET_PRESERVED_THEME_KEYS for layout/feature keys presets must not clobber), TestChat.tsx buildPreviewPublicConfig.
- 2026-07-15 BLOG — Added a three-post commerce-discovery cluster:
  `agentic-commerce-ecommerce`, `how-to-get-products-recommended-by-chatgpt`,
  and `ai-product-recommendation-chatbot`. Each has a 40–60-word Quick Answer,
  seven FAQ entries for schema, primary-source citations, internal cluster
  links, and an OpenAI-generated 1200×800 WebP hero. Added all three to the
  manually curated `public/llms.txt` guide list.
- 2026-07-15 DECISION — Rewrote all 231 sales-lead emails around a two-sentence relevance hook (verified observation → concrete website task), removed first-touch plan-limit copy, and split the shared pitch into e-commerce/catalog and service/organization variants. Unsupported buyer-behavior claims are rejected; 19 incumbent-chatbot leads acknowledge the existing assistant respectfully. Added a validated dry-run/repair/apply script. See sales-leads.
- 2026-07-15 FEAT — Configurator section animations: CollapsibleSection now uses framer-motion for smooth ease-in-out expand/collapse (height+opacity via AnimatePresence variants) with `staggerChildren` that propagates through MotionContext to SettingsGroup motion items (they inherit variant names, no own initial/animate → descend-into-place fade). overflow is hidden during the height animation, flipped to visible onAnimationComplete so Selects/popovers/tooltips aren't clipped. Section header gradient now glows from BOTH edges (`::before` left + `::after` right) and open sections get a 2px bottom border. reducedMotion="user" strips the transform descend. Gotcha logged: grouped `::before,::after` rules drop the shared block under Lightning CSS — see gotchas.md.
- 2026-07-15 BLOG — Added five non-cannibalising commerce guides:
  Google AI Mode shopping, semantic e-commerce search, conversational commerce,
  Magento AI-chatbot setup, and safe returns automation. Each includes a
  40–60-word Quick Answer, seven FAQ entries, internal links, primary evidence,
  and a text-free OpenAI-generated 1200×800 WebP hero; all five were added to
  `public/llms.txt`.
- 2026-07-15 FIX — Configurator section animation polish: (1) Collapse is now INSTANT (removed framer `exit`) — an animated height collapse shrinks the scroll content frame-by-frame, making the ResizablePanel clamp scrollTop and the sticky `backdrop-blur` toolbar repaint every frame = visible flicker/"refresh" when collapsing while scrolled. Verified: scrollTop stays put + no mid-height body on collapse. Expand still animates. Do NOT re-add `exit` to CollapsibleSection. (2) Card descend slowed to be noticeable: SettingsGroup item duration 0.3→0.45, y −8→−14; body staggerChildren 0.05→0.09, delayChildren 0.06→0.08, duration 0.3→0.36.
- 2026-07-15 TWEAK — Section accent border moved: the 2px border now sits under the section HEADER (with the glow) in the primary/accent color (`border-b-2 border-primary` on the open header), not at the section's bottom. Closed headers keep the neutral 1px `border-b`.
- 2026-07-15 FIX — Blog Markdown tables no longer rely on horizontal scrolling in the 42rem article column. `lib/blog.ts` adds column-count/layout metadata plus a `data-label` per cell; container-query CSS presents 4–6-column comparisons as compact labelled cards and stacks 3-column tables on phones. Verified at 320/375/414/768/1024/1440px with zero page or table overflow. See blog-and-seo.
- 2026-07-15 FEAT — LinkedIn planning gained 10 broader CX, operations, knowledge, trust, commerce, and accessibility drafts with a cohesive OpenAI-generated 1200×628 WebP visual series. Board cards now show only image/title/suggested order/drag handle; clicking opens a full-height right drawer with complete copy, metadata, copy actions, pipeline status, delete, and save. The idempotent `--new-only` seed mode puts the new set at Suggested #1–10 while retaining existing draft order and statuses. See linkedin-content.
- 2026-07-15 FEAT — Chat typography expanded from 6 to 24 free, open-source Google Fonts grouped into business, playful, serif, and monospace sections, including Baloo 2, DynaPuff, Mali, and Atma. The font picker is now a compact transactional dialog: it remains open while choices preview in the live chat, Keep accepts, and Cancel or dismiss restores the opening font. The redundant pangram was replaced by a 400–700 weight slider. `theme.fontWeight` is validated, saved, previewed, exposed in the public widget config, and applied at the ChatWindow root; visual theme presets preserve it. Optional font families are self-hosted by `next/font` without global preloading. See bot-config.
- 2026-07-17 BLOG — Added six intent-separated guides in two clusters: e-commerce GEO / IndexNow / AI-search measurement and knowledge-base preparation / hallucination prevention / pre-launch chatbot testing. Every article includes a 40–60-word Quick Answer, seven FAQ entries, primary evidence, deliberate cross-links, and an original text-free 1200×800 WebP hero. Added all six to the curated `public/llms.txt` list. See blog-and-seo.
- 2026-07-16 FIX — Quick-action prompt leak: when a quick action's hidden `prompt` differs from its visible `label`, the widget now wraps the sent message as `[Visitor clicked "<label>" — internal instruction, never quote or mention it: …]` (ChatWindow handleQuickAction) and buildSystemPrompt gained a QUICK ACTIONS rule: reply in terms of the label only, never reveal instruction text/URLs/slugs — including in failure apologies. Trade-off: the wrapper slightly pollutes the RAG retrieval embedding for these messages; the weak-retrieval rewrite path covers it.
- 2026-07-16 FEAT — "Show products" quick action (deterministic) + list-products-by-URL: new quick-action `action: 'products'` with a `query` field (search phrase OR store category/tag/collection page URL) — click renders product cards directly via /api/widget/search, no LLM round-trip (ChatWindow runProductsAction; localized note/empty copy). URL support lives at the shared point `searchCatalog` (lib/products/search.ts): an http(s) query first tries `listStoreProductsByUrl` (lib/commerce/index.ts) — Woo resolves the LAST path segment as a term slug (Store API categories list → `?category=ID`; tag via public `wp/v2/product_tag?slug=` → `?tag=ID`; permalink bases vary per language so never match on them), Shopify matches `/collections/<handle>` via Storefront collection query; magento/feed return [] and the slug words ("natalijos-hitas" → "natalijos hitas") become the search query. Because it's in searchCatalog, the LLM search_products tool + voice + preview all gained URL support too. Verified live against homebynb.lt (tag 3103 → 24 products) + unit tests with injected fetch.
- 2026-07-16 TWEAK — Product cards narrowed 80%→72% (ProductCards.tsx, more next-card peek). Fixed curved-header "See all" list view: the body's rounded top corners showed through as notches above the list overlay's pink sub-header bar. Fix: ChatWindow drops the body's curved top radius (`borderRadius: listProducts ? '0' : …`) while the full-screen list overlay is open — the overlay covers the whole body so the curve serves no purpose then. Classic/floating styles unaffected. Diagnosed via element tinting: the notch = whatever sits behind the body's rounded corner (header or root bg).
- 2026-07-16 FEAT — Mesh-gradient chat backgrounds: 8 presets (lib/widget-gradients.ts) as tiny inline SVG data-URLs (blurred circles over a base) stored in the EXISTING theme.backgroundImageUrl — zero schema/widget changes; opacity slider, LogoUpload preview/remove, and preset-clearing all apply as-is. Selecting also sets theme.backgroundColor to the preset base (pre-image paint + drives dark-body text contrast, so dark gradients get light text). Swatch grid sits above the background-image upload in Chat surface. GOTCHA: encodeURIComponent leaves ' ( ) literal, which breaks UNQUOTED CSS url(...) interpolation — data URLs must escape them (%27 %28 %29); ChatWindow's backgroundImage url() is now quoted too.
- 2026-07-16 TWEAK — Gradient presets expanded 8→20 (lib/widget-gradients.ts): +6 subtle lights (near-white bases, 2-3 gentle tints: mist/linen/pearl/mint/vanilla/cloud) and +6 deep darks (near-black bases, 2-3 muted glows: obsidian/abyss/nocturne/charcoal-ember/night-forest/black-plum). Configurator swatches switched from an 8-col grid to a horizontal scroll row (h-16 w-12 shrink-0, thin scrollbar).
- 2026-07-17 FEAT — Composer-placed call button + voice-mode visuals: new `theme.callButtonPlacement` ('header' default | 'composer'). Composer placement hides the header call button but still MOUNTS VoiceCallButton with new `appearance='none'` (display:contents wrapper so no phantom flex gap) — it owns the ElevenLabs session; onReady now exposes {start, end} (startVoiceRef/endVoiceRef in ChatWindow). Composer right slot: phone button (callButtonColor) when field empty → morphs into send while typing; radius follows the field radius (capped 19 = circle). Disabled dictation-mic placeholder sits left of it (composer placement only, no STT wired yet). During a call: composer becomes a waveform bar (.cbz-wave-bar, deterministic delays, data-speaking boosts amplitude) + red end pill; a brand-tinted SiriOrb (components/widget/SiriOrb.tsx) pins above the transcript (flex-shrink-0 strip; MessageList is flex-1 overflow-y-auto so bubbles scroll UP TO the orb) for the whole call — welcome screen is skipped while call active. Orb shows for calls in BOTH placements; wave bar only in composer placement. Configurator: "Call from message field" toggle (voice-enabled only); Compact toggle hidden when composer placement. Known gap: mic-denied/call errors are silent in composer placement (header mode showed inline text).
- 2026-07-17 FEAT — Voice polish trio: (1) composer-placed call errors now surface — VoiceCallButton `onIssue` emits codes (mic-denied/unavailable/error), ChatWindow maps to localized copy, Composer renders a notice line above the bar. (2) Whisper dictation, plan-gated: `dictation` entitlement (free=false, all paid=true; bullet in plans-catalog Starter cascade), PublicBotConfig.dictation, POST /api/widget/transcribe (origin+plan gate, 6-cap rate limit, 5MB cap) + /api/preview/transcribe (authed, same gate) → lib/ai/transcribe.ts (whisper-1 direct fetch, language hint). Composer mic: MediaRecorder (browser-default container: webm Chrome / mp4 Safari — Whisper takes both), 60s auto-stop, red pulsing while recording, spinner while transcribing, text appends to field. `transcribe` is OPTIONAL on ChatTransport; widget+preview implement it; button shows only when config.dictation && transport.transcribe. canUseDictation threaded ConfigForm→TestChat from all four configure pages (ent.dictation). widget.js iframe already allows microphone. (3) Call orb now sits in a brand-tinted halo (radial glow + 1px ring + outer shadow, intensities differ for dark bodies; orb bg pure white/#0d0d12 + 50% brand color mixes) so it reads on any background — the plain orb washed out on light themes.
- 2026-07-17 FIX — SiriOrb rebuilt on boring CSS: the reference implementation (registered @property angle animation + backdrop-filter + mix-blend-mode overlay + contrast() filter) rendered wildly differently across browsers — Chromium showed a living orb, Safari a static neon blob (angle animation frozen at 0° stacks all conic layers). New version: static layered radial-gradient base + a blurred conic layer spun with plain transform rotate + a soft top-light/vignette ::after. Renders identically everywhere, still looks like an orb with animations off (reduced motion). Same props/API. Preview and live widget were never different — it was the browser.
- 2026-07-17 TWEAK — SiriOrb v3: particle sphere on 2D canvas (900-dot fibonacci sphere, orthographic projection, transform spin) replacing the CSS-gradient orb — dim brand-dust globe + vivid turbulent rim (light tint top → deep shade bottom, all derived from the brand hex in JS; canvas can't use color-mix). New `activity` prop (0–1) drives spin speed, rim scatter, dot size and alpha with eased transitions — ChatWindow maps callState: speaking→1, connecting→0.55, listening→0.22, so the orb calms while the visitor talks and comes alive when the agent speaks. Halo wrapper removed (the particle rim self-defines). Reduced motion renders a single static frame. Dot radius scales with canvas px so any size works.
- 2026-07-17 TWEAK — Orb active state punchier (displacement 0.11→0.17, spin +0.55→+0.75, turbulence clock ×(1+level·0.9) so particles move further AND jitter faster while the agent speaks). Dictation recording now turns the message field into the same waveform-with-red-stop as live calls (waveField helper shared by both states in Composer; stop → Whisper transcribe → text appends). Verified end-to-end by stubbing getUserMedia with canvas.captureStream() in the browser.
- 2026-07-17 FIX — Orb "wild spinning" bug: scaling the turbulence clock by activity (`t = now * factor(level)`) made every particle's phase leap whenever the eased level changed (absolute time × changing factor). Clock and spin reverted to constants; activity now scales ONLY the displacement amplitude. Rule: never multiply absolute time by a state-dependent factor in an animation — integrate speed via dt instead (like rotY).
- 2026-07-17 FEAT — Added first-class Verskis commerce support after `mobel.lt` was incorrectly tested as WooCommerce: auto-detection switches and persists the provider, validation counts public product sitemaps, and live HTML search/category parsing produces current product cards without credentials. It initially launched keyword-only (semantic sync added later the same day); order lookup remains unsupported. Live-verified against mobel.lt (1,951 products). See commerce.
- 2026-07-17 FEAT/FIX — Added and published a specialized Furniture Store system prompt, assigned it to the Mobel and 3IMIS/Chafox Verskis bots, and hardened the Verskis data path it depends on. The prompt keeps a hard/preference/unknown constraint ledger across turns, performs strict unit/dimension reasoning, verifies color/material/orientation via details, and avoids global “no match” claims after attribute searches. Opaque product ids resolve through candidate/card URLs for full attributes, while parser boundaries exclude alternative/related-product attributes and `results_total` excludes recommendation carousels from search matches. Live-verified on mobel.lt product details. See commerce and bot-config.
- 2026-07-17 FIX — Mobel furniture recommendations now arrive on the first useful turn and render as cards instead of prose hyperlinks. Verskis search concurrently enriches up to 12 candidates with main-page attributes, so a base `sofa` search exposes exact `Spalva: Kapučino` on CA95931 without four serial detail batches; empty narrow searches return an explicit base-category retry instruction. Restored opaque numeric model ids (URLs had encouraged Markdown links), remapped them internally for details, made the furniture prompt search immediately instead of asking for unspecified dimensions, and streamed `display_products` cards into the UI before final prose completes. Browser-verified the exact Lithuanian request: TI28201 + CA95931 cards in ~2.7s, first turn. See commerce and bot-config.
- 2026-07-17 FEAT — Verskis joined the full Smart product search pipeline: Sync catalog is visible, product sitemap pages crawl with 16 workers into rich category/description/brand/attribute docs, embeddings/upsert-prune use the existing Supabase vector schema, semantic results hydrate live price/stock from stored product URLs, nightly cron includes already-indexed Verskis bots, and configurator preview now uses the same semantic `searchCatalog` path as the live widget. Verskis skips generic AI recipient tagging to keep first sync under Vercel's 300s cap; full Mobel source crawl live-verified at 1,951 unique products in ~69s. Stale cross-store index URLs fall back safely until sync replaces them. See commerce and gotchas.
- 2026-07-17 FIX — Product matching is now Lithuanian-inflection and field aware. Main product color outranks component color/general prose, so Mobel's 11 `Spalva: Balta` dining chairs are retrieved for `baltos kėdės`; canonical catalog-form queries were also added to the commerce/furniture prompts. See commerce and gotchas.
- 2026-07-17 DECISION — Provider isolation is now a hard commerce invariant. Added one commerce profile per provider (catalog fetch/enrichment policy, matcher RPC, capability check, hydrator, detail-reference conversion, guards, optional guidance), moved Verskis field-aware ranking to `match_products_verskis`, restored the neutral matcher for Woo/Shopify/Magento, and added isolation tests plus a dedicated extension checklist. Shared search, sync, and AI tools now orchestrate profiles instead of branching on a provider/store. See commerce-provider-profiles.
- 2026-07-17 FIX — Verskis broad browsing now returns up to 20 verified cards without changing other providers: its profile owns 32-row candidate overfetch, 20 structured details, canonical sofa-bed query normalization, and deterministic title-family/attribute completion. Live hydration uses eight ordered workers with one retry and parses the visible main price when configurable-product JSON-LD omits `offers.price`; this restored Mobel CA60511/GA76681. Fresh searches cannot mix prior-turn cards. Live checks: 20/20 green dining chairs, and exactly all 9 straight sofa-beds (no normal sofa or corner sofa). See commerce and commerce-provider-profiles.
- 2026-07-17 DECISION — Documented the provider-adoption boundary: stale-card prevention, early product-card streaming, preview/live `searchCatalog` parity, and missing-data safety are universal; query normalization, hydration overfetch, larger detail budgets, structured display completion, custom matchers, and HTML parsing remain explicit provider opt-ins. Keep Verskis at 20 cards; use pagination rather than unbounded categories because live hydration/model/UI cost dominates the cheap DB row fetch. See commerce-provider-profiles.
- 2026-07-17 FIX — Restored the production Edge build for `/api/llm/[publicKey]`. Prompt imports had traversed the aggregate provider registry into Verskis catalog hashing (`node:crypto`), and the existing aggregate commerce import also exposed SSRF DNS/network native modules. Split prompt-only provider metadata into `provider-profiles/guidance.ts` and pure commerce gates into `commerce/capabilities.ts`; server registries re-export them for compatibility. `npm run build` now completes page-data collection without native Node Edge warnings. See commerce-provider-profiles and gotchas.
- 2026-07-17 FIX — Kept the asynchronously populated per-language voice Select controlled for its full lifetime. Its empty value is now Base UI's supported `null` sentinel instead of `undefined`, preventing the enable-voice flow from switching uncontrolled → controlled when the first curated voice is auto-selected. Added a regression test for the exact toggle/fetch/selection sequence. See voice.
- 2026-07-17 FEAT/FIX — Brought ElevenLabs product recommendations to text-chat parity: voice search still uses shared semantic `searchCatalog`, but now returns candidates + provider-budgeted structured facts without showing them; a new `display_products` client tool renders only latest-search ids the voice model verifies against every hard constraint. Voice tool/prompt instructions preserve combined dimensions/color/material/etc., reuse provider-specific query/display guidance, and force agent re-sync with hash `v29`. Lithuanian/English spoken measurements normalize to digit transcripts and centimetre catalog queries (`du metrai ant metro aštuoniasdešimt` → `2 m × 1,8 m` on screen, `200 cm × 180 cm` for search). See voice and commerce-provider-profiles.
- 2026-07-17 FIX — Restored voice-call startup after ElevenLabs rejected the new `display_products` client tool's array parameter schema. Selected product ids now cross the scalar client-tool boundary as JSON text, with backward-compatible parsing and latest-search validation in the browser. Voice-token routes now log bounded upstream validation details instead of hiding every sync failure behind an unexplained 502. See voice and gotchas.
- 2026-07-18 BLOG — Added a six-post AI-for-e-commerce pillar cluster: broad store use cases, Shopify Sidekick/Magic versus storefront helpers, product-description operations, personalization, inventory forecasting, and fraud detection. Each intent owns a different job, includes a 40–60-word Quick Answer, seven FAQ entries, current primary sources, deliberate internal links, and two original 1200×800 WebP visuals (editorial cover + text-free process infographic). Added all six to `public/llms.txt`. See blog-and-seo.
- 2026-07-19 INGEST — room-visualizer.md added: furniture "see it in your room" feature (flag flow, tray/studio widget UI, /api/widget/visualize check order, Gemini module, preview-invisibility gotcha). Built on worktree branch worktree-room-visualizer.
- 2026-07-19 DECISION — Room visualizer renders are never stored (data URLs only, cap counter is the sole DB write); cap consumed only after successful Gemini call. Why: privacy + zero schema surface for v1.
- 2026-07-20 INGEST — Added nine researched furniture-store prospects to the owner sales pipeline (five user-supplied net-new domains plus Baldai1, Gintaro Baldai, LONAS, and Baldų Perlai). The idempotent migration preserves the existing Guru Baldai row, canonicalizes Sofaforma's domain, records platform/chatbot fit, and keeps unsupported PrestaShop/CS-Cart/custom catalog integration gaps explicit. See sales-leads.
- 2026-07-20 INGEST — Added Mobel as a demo-backed furniture prospect with the existing active Verskis demo and its verified 1,951-product catalog recorded as qualification evidence. Kept the lifecycle at Ready until actual outreach. See sales-leads and commerce.
- 2026-07-20 DECISION — Room visualizer is demo-bots-only until GA: public widget-config zeroes the flag and /api/widget/visualize 403s unless the bot's org has is_demo; configurator toggle now only renders in the demo-bot editor (showRoomVisualizer prop). Why: ship to main without exposing an unpriced Gemini-cost feature to clients. See room-visualizer.
- 2026-07-20 DECISION — Recorded the public SEO/GEO remediation architecture and task plan after clean mobile Lighthouse verification: render-visible critical copy, one-at-a-time adaptive hero media, bounded images/fonts, server-paginated blog, readable noindex controls, factual sitemap dates, explicit publisher/methodology evidence, and a final accessibility phase. Baselines: homepage Performance 61/LCP 11.3 s/~10.5 MB; blog Performance 63/LCP 10.8 s. The supplied Best Practices 58 was extension contamination; clean score 100. See blog-and-seo and the linked spec/plan.
- 2026-07-21 FIX — SEO remediation Phase 1 made all rendered landing headings/copy server-visible, removed Lenis and its page-long RAF loop, retained native reduced-motion-aware anchors, and fixed `HeroVideo`'s server-video/client-poster reduced-motion hydration mismatch with a poster-first hydration snapshot. No-JS/reduced-motion/anchor Playwright checks pass; local optimized-build median Performance improved 61→79 and lab LCP 11.26s→5.83s, with ~7.96 MB video transfer remaining for Phase 2. See blog-and-seo and gotchas.
- 2026-07-21 PERF — SEO remediation Phase 2 replaced simultaneous 3.9–4.1 MB hero downloads with responsive poster-first H.264 derivatives and a tested intro-then-loop state machine. Reduced-motion, Save-Data, and 2G paths make zero video requests; mobile media is 584 KB combined and local mobile Lighthouse transfer fell 10.34→2.31 MB. The related CTA shimmer is now CSS-only after reduced-motion testing exposed its hydration branch. See blog-and-seo and gotchas.
- 2026-07-21 PERF — SEO remediation Task 2.3 replaced 11 simultaneously mounted 840×1360 PNG showcase designs with ~350 KB of source WebPs, bounded `next/image` rendering, an idle-deferred forward neighbor, and CSS outer-wing skeletons. Playwright covers the three-request ceiling plus click/wrap/swipe behavior; local mobile image transfer fell 1.34→0.30 MB and total transfer 2.31→1.28 MB with zero CLS. See blog-and-seo.
- 2026-07-21 PERF — Completed SEO remediation Phase 2: responsive How-it-works delivery, two global font preloads, one nav-logo request, and a six-second+idle landing-widget policy with a first-click proxy while `/present` stays immediate. Final local mobile medians: Performance 85, LCP 4.30s, 0.94 MB, 38 requests, zero CLS (baseline 61/11.26s/10.78 MB). Remaining simulated LCP work is the measured 35 KB render-blocking CSS follow-up in Task 6.5. See blog-and-seo and gotchas.
- 2026-07-21 PERF — Completed SEO remediation Phase 3: 51 posts now span five server-rendered 12-card archive pages with canonical metadata, crawlable anchors, and hard 404s for duplicate/malformed ranges; numbered archives stay out of the sitemap because every article URL is already listed. Responsive covers, compact mobile cards, a static header texture, no repeated card avatars, route-scoped Plus Jakarta preload, and disabled speculative archive/home prefetch produced a local mobile median of Performance 91, LCP 3.46s, 0.41 MB, 36 requests, 13 image requests, and zero CLS. See blog-and-seo.
- 2026-07-21 INGEST — Added the authoritative SEO/GEO playbook for all future public and blog work: one-intent ownership, primary/first-party evidence, honest author/method claims, visible server content, contextual link graph, non-magical FAQ/llms/schema treatment, image/performance budgets, canonical/index rules, and pre/post-release measurement. Corrected the blog Wiki’s prior implication that article structure guarantees AI citations. See seo-geo-playbook and blog-and-seo.
- 2026-07-21 FEAT — SEO/GEO phase 4 (indexing & metadata): noindex,nofollow on auth/app/owner/embed/present boundaries; robots.txt slimmed to /api,/app,/owner so noindex stays crawler-readable; sitemap freshness is now real (updated ?? date per article, fixed git-derived LEGAL_UPDATED for privacy/terms, listings omit lastModified, zero request-time dates); route-specific OG/Twitter on blog/paginated/privacy/terms + article og modified_time; FAQ-schema comments de-hyped (no rich-result claim); new tests/unit/public-seo.test.ts + structured-data.test.ts (9 tests). Archive pages stay out of the sitemap (phase-3 decision). Pending: prod smoke + Lighthouse + GSC resubmit after deploy; owner confirmation of legal-review dates. See seo-geo-playbook.
- 2026-07-21 FEAT — SEO/GEO phase 5 (engineering + curation): controlled 6-topic taxonomy (topic: frontmatter on all 51 posts, lib/blog-topics.ts) with /blog/topics/[topic] hubs (unique intros, pillar-first, chips on /blog, byline chip on articles, sitemap'd); curated related: for the 28 missing posts + same-topic fallback replaces global recency; link graph repaired (12 orphans got contextual inbound links, 2 posts got 2nd outbound); npm run audit:blog (errors fail CI-style, warnings = editorial review, matches Appendix A's 29 uncited list); identity pages /about, /authors/eimantas-kudarauskas, /editorial-policy (AI-assistance disclosed honestly), /review-methodology (COI stated) + article Person schema enriched (author URL, sameAs, worksFor). Owner gates remain: 5.1 bio fact-check, 5.5 29-post citation review, 5.6 evidence assets, 5.8 GSC baselines. See seo-geo-playbook and blog-and-seo.
- 2026-07-21 INGEST — SEO/GEO phases 1–5 merged to main and deployed. Production smoke matrix PASSES (all 11 public routes: self-canonical + per-route og:url, indexable; /login + /embed serve noindex,nofollow; robots.txt = /api,/app,/owner only; sitemap 65 unique URLs incl. hubs/identity pages, zero build-time stamps; article Person schema live). Lighthouse mobile (1 run, local machine, not a clean lab): home perf 67 / SEO 100 / BP 100, LCP 6.0s (was 61/11.3s); blog perf 75 / SEO 100 / BP 100, LCP 4.8s (was 63/10.8s). Perf improved massively but home LCP still above the 3.5s release target — remaining perf work tracked in the plan. GSC sitemap resubmission = owner.
- 2026-07-21 INGEST — Task 5.5 citation review COMPLETE: all 29 uncited posts (plus the stats post's 20-source audit and 22 already-cited posts' spot fixes) reviewed across 6 owner-approved batches. ~60 primary-source citations added; recurring fixes: Tidio per-conversation (not per-seat), Zendesk AI included/usage-billed (not add-on), invented multipliers (3-4x, $749 tier, 2-3x BFCM) removed or corrected, all voice/language claims scoped to EN/LT, GDPR post cites real articles, stats traced to CSA/Baymard/Gartner/Shopify originals. 9 posts recorded as deliberate citation-free exceptions (CITATION_EXCEPTIONS in audit script). npm run audit:blog now: 0 errors, 0 warnings. Remaining phase-5 owner items: 5.6 evidence assets (deferred, product too new), 5.8 GSC baselines. See seo-geo-playbook.
- 2026-07-21 INGEST — Task 5.8 baselines recorded from owner's GSC exports → new wiki page seo-baselines.md: 32/65 sitemap URLs indexed (report lags to Jul 10), 18 clicks/~1,375 impressions over 3 months (real clicks ≈ LT brand searches at pos 2.6), two impression magnets at unclickable positions (conversational-ai-vs-chatbot 568 imp @ pos 83; zendesk-ai-review 507 @ 51 — top refresh priorities), 8 page-1 rankings with zero clicks (low volume, recheck at 28d). Checkpoints: 2026-07-28 and 2026-08-18. Monthly 10-prompt GEO visibility sample defined (manual, no scraping). Bing verification pending owner decision. Phase 5 gate now closed except nothing — all items done; program remainder = phases 6 and 7.
- 2026-07-21 FEAT — SEO/GEO phase 6 (secondary delivery & crawler compat): llms.txt rewritten to descriptive markdown links (65 validated links; hubs + identity pages added; facts checked against plans-catalog; wiki note that Google ignores it); GA switched to lazyOnload (owner chose delayed over consent-gated); security headers added in next.config.ts (nosniff, referrer-policy, minimal permissions-policy w/ microphone=self for voice, x-powered-by off, CSP as REPORT-ONLY — promote only after a clean report period; deliberately no frame-ancestors so /embed stays embeddable); IndexNow is delta-only via public/indexnow-manifest.json (prebuild manifest, ping diffs against live manifest, unchanged build submits 0, --force preserved, 4 unit tests); blog .article CSS route-scoped (homepage main chunk 168→157KB raw, remaining bundle is Tailwind utilities — further split poor ROI). Post-deploy verification pending: GA events, CSP report-only period, delta ping behavior, BP-100 Lighthouse. See seo-geo-playbook and blog-and-seo.
- 2026-07-21 FIX — IndexNow had been silently 403ing (UserForbiddedToAccessSite) on EVERY deploy since the feature shipped — the fail-safe logging hid it in build logs. Fix: rotated the key (their validator negative-caches failures) + excluded .txt/.json/.xml/media from the auth proxy so crawler fetches of the key/manifest never depend on Supabase. Now verified live: single URL → 202, full 65-URL push → 200, and a code-only deploy submits ZERO URLs (delta manifest works). Also: first report-only CSP finding fixed (GA4 beacons hit region1.analytics.google.com + stats.g.doubleclick.net — added to connect-src); post-fix Lighthouse: BP 100, SEO 100, perf 80 (best yet), LCP 4.1s, TBT 50ms. GA lazyOnload verified in prod (gtag loads after window load, dataLayer populated). Phase 6 gate closed; signup-conversion GA events remain to be spot-checked during normal use; CSP stays report-only pending a quiet observation period. See seo-geo-playbook and SECURITY.md.
- 2026-07-21 FIX — Two landing regressions from the perf phases: (1) the deferred proxy launcher was a hardcoded brand-orange button, visibly swapping to the real configured launcher (#c04b0c, filled chat icon) when widget.js loaded ~6s in — the landing page now server-fetches the bot's launcher theme (getLandingBot in lib/platform-bot.ts) and the proxy renders pixel-identical (color/icon/size/offsets; circle style only — pill would still mismatch for 6s); (2) the hero loop video was only requested AFTER the intro ended (frozen last frame → visible pop) — HeroVideo now prefetches the loop when ≤4s of intro remain and starts it exactly at onEnded gated on canplaythrough (refs handle either event arriving first). Verified on a local prod build: intro 5.0/5.0 ended with loop already at t=0.7s; proxy SSR markup matches the live launcher's computed style. See widget-and-embed.
- 2026-07-21 FIX — Follow-up on the landing handoffs after owner re-test: (1) the intro→loop crossfade now STARTS 0.6s before the intro ends (CROSSFADE_LEAD_SECONDS) with a 700ms fade, so it blends two moving videos instead of popping from a frozen last frame — traced: intro at 4.71→5.04s while opacities swap; (2) the proxy launcher previously unmounted on script load / reveal event, leaving ~150-350ms with no visible button while the real launcher ran its 0.2s fade-in behind a config fetch — events and timers both proved racy, so WidgetEmbed now POLLS the real launcher's computed opacity and unmounts only at ≥0.99 (10s cap; widget.js dispatches cbz:launcher-revealed to start the watch). Playwright trace: 0 samples without a fully-visible button. Gotcha: rebuilding .next while next start is running corrupts the chunk map (HTML referencing 500ing chunks) — rm -rf .next + clean rebuild fixes it. See widget-and-embed.
- 2026-07-21 FIX — Widget loader polish after owner re-test: (1) purple launcher flash on reload = config fetch occasionally slower than the 1.5s reveal safety net → widget.js now caches {theme, avatarUrl} per botKey in localStorage (cbz_theme_<key>) and paints the accent color at creation on repeat visits (greeting settings deliberately NOT cached so scheduling stays tied to fresh config); (2) ghost greeting 'pop' with no visible bubble = SPA nav tore down the widget DOM but the pending greeting timer survived and WebAudio needs no DOM → showProactiveGreeting now bails when its element is disconnected; (3) added a one-instance-per-page guard (skip init if [data-cbz-wrapper] already mounted) so duplicate script evals can't double the surfaces or the sound. 2 new loader regression tests (11 total). Playwright-verified: reload first-paint = rgb(192,75,12), single wrapper. NOTE: the landing bot's greeting frequency is 'every_page' by config — every reload/page legitimately replays the greeting + pop; switch to once_per_session in the configurator if that annoys. See widget-and-embed.
- 2026-07-21 DECISION — Hero brand marquee now shows CLIENT-relevant platforms (WooCommerce/Shopify/Magento/WordPress/Squarespace/Webflow as monochrome simple-icons marks + Wix/Verskis wordmarks + a Product-feeds glyph) under 'Works wherever your store runs', replacing the internal-stack list (OpenAI/ElevenLabs/Stripe/Supabase) that meant nothing to store owners. Marks live in components/landing/brand-marks.ts (CC0 simple-icons paths, fill=currentColor). Gotcha discovered while verifying: 'next start' re-titles its process to 'next-server', so pkill -f 'next start' leaves orphaned servers serving stale builds — the earlier 'corrupted chunk map' mystery was exactly this; kill with pkill -f 'next-server'. See widget-and-embed and gotchas.
- 2026-07-21 FIX — Landing widget auto-closed itself + re-showed the greeting after a manual open. Root cause: WidgetEmbed's idle pre-load setTimeout (6s) was never cancelled when the user clicked the proxy button, so it fired, re-entered loadWidget(), and — because openWhenReady was set from the click — ran existingLauncher.click() which TOGGLED the open widget shut; the greeting scheduled at widget-load+3s then fired into the now-closed state (isOpen false) looking like a fresh load. Fix: clicking the proxy (openAfterLoad) now clears the idle timer/idleCallback, the existing-launcher open is gated on THIS call's openAfterLoad + aria-expanded!=='true' (never toggles an open widget), and openRealLauncher got the same not-already-open guard. Playwright-confirmed: widget opens and stays open, no greeting after. 2 regression tests in widget-embed.test.tsx. See widget-and-embed.
- 2026-07-23 BLOG — Added three non-overlapping commercial-intent guides: furniture-store AI product guidance, Shopify Inbox versus dedicated AI, and live chat versus AI for e-commerce. Each has a 40–60-word Quick Answer, seven FAQs, explicit related links/backlinks, current source evidence, and two original 1200×800 WebP visuals (cover + process diagram). Corrected the older Shopify comparison's stale Loqara order-lookup claims: Shopify product search is supported, Shopify order lookup is not. See blog-and-seo and commerce.
- 2026-07-23 OPS — Verified the first real Sales Leads outreach send end to end: select the highest-score Ready row (then name), send its stored subject/body unchanged from `hello@loqara.com` with the branded signature, confirm recipient and subject in Hostinger Sent, and only then conditionally advance that row to Email sent. API sends must append the signature explicitly; webmail's configured signature is not automatic. See sales-leads.
- 2026-07-23 FEAT — Room visualizer is now a paid ADD-ON (€29/mo, 100 renders/month pool + existing 5-per-conversation cap; overage deliberately unadvertised): VISUALIZER_ADDON in plans-catalog, Stripe item mirrored to organizations.visualizer_addon (shared setAddonItem in lib/stripe/manage), STRIPE_PRICE_VISUALIZER_MONTH env (sandbox price created; LIVE price pending owner), migration 20260723090000 (addon column + visualizer_usage table + atomic increment RPC — applied to prod). Gates: widget-config hides the tray unless (is_demo OR addon) AND pool left (quiet monthly disappearance); visualize route mirrors entitlement + checks pool BEFORE model spend + consumes via RPC (demo orgs exempt). Engine choice config.roomVisualizerModel: nano-banana-pro (default) or gpt-image-2 (OpenAI images/edits, raw-fetch multipart, quality=high — input_fidelity REJECTED by gpt-image-2, verified live). New configurator section VisualizerSection (replaces the Commerce-section row; canUseVisualizer prop), BillingPanel card + confirm dialog + server action, landing Add-ons card, MRR counts it. 12 visualize-route tests. See room-visualizer and plans-and-entitlements.
- 2026-07-23 INGEST — Visualizer add-on went fully live: owner created the LIVE Stripe product ('Loqara Product Visualizer', €29/mo) + set STRIPE_PRICE_VISUALIZER_MONTH on Vercel; user-facing name is now 'Product visualizer' everywhere (internal roomVisualizer identifiers unchanged). Also fixed a latent changeBasePlan bug: base-item lookup now excludes ALL add-on prices (voice-only exclusion could have swapped the visualizer item as 'base'). Prod-verified post-deploy: demo bot keeps the tray, add-on-less client stays hidden, visualizer_usage empty. NOTE: the owner's manual live product lacks the loqara_addon=visualizer metadata the setup script matches on — running stripe-setup.mjs --live would create a duplicate product; add that metadata in Stripe first or skip re-runs. See room-visualizer.
- 2026-07-23 DECISION — All paid add-ons (Voice + Product visualizer) are ALWAYS available for internal orgs — the owner's own chatbot (is_platform) and every demo bot (is_demo) — with the paywall only for real client orgs. New isInternalOrg(org) helper in lib/entitlements.ts (is_demo || is_platform) is the single predicate; applied in all 3 runtime gates (widget-config voice+visualizer, voice-token, visualize — platform was previously unrecognized so the owner's landing chatbot would have been paywalled) and internal orgs skip the visualizer render pool. Configurator: owner/chatbot now passes canUseVisualizer (had only canUseVoice); demo pages already pass both. Add-on visibility in the live widget still requires the owner to enable the feature in config — the gate only removes the paywall. See room-visualizer and plans-and-entitlements.
- 2026-07-23 DECISION — Redesigned the conversation-list cells (components/client/TranscriptView.tsx, shared by client + owner bot views). Dropped the truncated conversation-id + copy button from each cell (the id/visitor-id is on the transcript header once opened). Two cell variants: regular, and 'needs attention' — the latter gets a left accent bar + soft destructive edge washes + bg tint (.attention-cell in globals.css, same vertical-field idea as the config-section header glow but red + always-on) and a labeled 'Needs attention' pill with a warning icon, replacing the easy-to-miss bare red dot. The AI-handling success_score (1–5) now renders as a labeled RatingBadge (Success/Mixed/Needs work + n/5) bottom-right where the id used to be — chose success_score as 'chat rating' since it's the only always-present per-conversation rating (thumbs feedback is sparse, negative-only rollup). Channel pill shortened to 'Chat'/'Voice' to fit the two-pill top row. needs_attention still = had_fallback OR a 👎 in the thread. See widget-and-embed.
- 2026-07-23 FEAT — Added layered visitor-abuse protection after reviewing real harassment/spam and prompt-extraction sessions: deterministic high-confidence detection, service-only bot+visitor 24-hour blocks, triggering-message audit, chat/model/handoff stop, blocked white/red widget, blocked voice-token minting, and ElevenLabs `end_call`. The two explicitly reviewed Loqara visitors were manually blocked for the first 24-hour window. Also closed cross-visitor conversation-id attachment by scoping resumed chats to visitor id. See visitor-abuse-protection.
- 2026-07-23 FEAT — Conversation review now shows a compact Blocked card for active visitor blocks with the exact expiry plus Unblock and Extend block controls. Mutations re-authorize the selected bot/conversation through the signed-in RLS client before a service-role write; extension adds 24 hours after the remaining expiry and uses compare-and-swap retry protection. See visitor-abuse-protection.
- 2026-07-24 FIX — "Show products" quick actions with a store page URL now really show that page: (1) a URL embedded anywhere in the query text is detected (previously only queries STARTING with http hit the URL path — "Show first 10 products from this page <url>" silently fell through to semantic search returning loosely-related items), default raised to first 20; (2) Woo URL listing now scrapes the page HTML for `data-product_id`/`add-to-cart` ids in DOM order and hydrates them via Store API `include=`, so results match the page top-to-bottom exactly and custom pages (karakara.lt /naujienos/ — not a category or tag term) resolve at all; term category→tag resolution remains the fallback. Page fetch is same-host-as-store only (queries can carry visitor text). Live-verified against karakara.lt (both /naujienos/ and /produktai/aktyviam-gyvenimui/ match the site 1:1). See commerce.
- 2026-07-25 DESIGN — Installed the impeccable design skill (global, `~/.claude/skills/impeccable`, v4.0.2) and ran its `init`: `PRODUCT.md` at the repo root is now the durable product record every impeccable command reads (four ranked audiences, positioning = chat+voice+catalogue+order-lookup+visualizer in one embed, real plan numbers, and an explicit list of what future work must NOT fabricate — no testimonials/logos/case studies/accuracy numbers). Live-mode config at `.impeccable/live/config.json` (targets `app/layout.tsx`; no CSP to patch). Two landing candidates then built on branch `landing-redesign` behind a fixed 1/2 switcher: `/v1` = "the studio" (blackout ground, saturated paper sweeps, gaffer-tape actions, generated studio imagery via gpt-image-2, scroll-scrubbed contact sheet + a scroll-driven "exposure" that lights the retrieved sources before the answer plate is un-flagged) and `/v2` = "the glazier's wall" (matte-black glazing bars drawn by grid gaps, seeded-white glass, cobalt/amber/oxblood reserved for what matters, existing feature screenshots + chatviews framed as panes that clear from frosted). Both reuse real product truth (`PLANS`, `FAQ`, `getLandingBot` + the real `WidgetEmbed`) rather than re-authoring claims; the demo conversation in `/v1` is labeled synthetic. The live landing at `/` is untouched; `(preview)` is `robots: noindex`. Gotcha found and recorded: branching render output on `useReducedMotion()` hydrates a mismatch — see gotchas.
- 2026-07-25 DECISION — Owner review of the two landing candidates: `/v1` (the studio) rejected outright, `/v2` (the glazier's wall) rejected except for ONE component — the three-step setup bay. Both candidate routes, their components and the generated studio imagery are deleted; the live landing at `/` is what ships. Ported the approved component only: `components/landing/SetupPanes.tsx` replaces the three rounded white how-it-works cards with a single matte-black frame whose glazing bars are the grid's own gaps (true thickness at every width, restacks to one column), each step's existing `how-step-*.webp` frosted until scrolled to and then clearing to daylight, hover backlighting the pane, and a cobalt/amber/oxblood accent per step. It inherits the landing's own type (Geist) rather than the candidate's face so it reads as part of `/`, and the glass-world copy puns ("Glaze it to your brand", "Fit the pane") were dropped for plain step copy since the surrounding page has no glass world to refer to. `use-reduce.ts` moved to `components/landing/` — still the hydration-safe reduced-motion read (see gotchas). Kept from the exercise: `PRODUCT.md` and the impeccable install.
- 2026-07-25 BLOG — Added four specific-use-case acquisition guides: verified electronics compatibility, technical B2B buyer qualification, optional pre-return troubleshooting, and non-medical beauty/skincare guidance. Each owns a separate intent, uses a 40–60-word Quick Answer, seven FAQs, explicit related links plus contextual backlinks, current first-party evidence, and two original 1200×800 WebP assets (editorial cover + realistic use scene). Durable product boundaries are explicit: Loqara does not invent compatibility/suitability, bind quotes or account price, deny/execute returns, diagnose, prescribe, or guarantee allergy safety. See blog-and-seo.
- 2026-07-25 DESIGN — FAQ rebuilt as a two-column section (intro + illustration left, all 11 questions right) after an owner-supplied reference. Questions live in ONE bordered panel (not separate cards) whose rows are `flex: 1 1 auto`, so the panel stretches to the left column's height and distributes the leftover space across the rows — measured bottom-edge delta is 0px in every open/closed state at 1440 and 1920. Collapsed that means 11 rows of 65px; open one and the rest give the space back (55px) while the total stays put. Open row gets a warm tint. New asset `public/landing/faq-illustration.webp` (26KB, square crop of a 3:4 generation) from Higgsfield GPT Image 2 — soft isometric frosted-glass chat panel + document rack in the brand orange, deliberately text-free and people-free. Measured finding worth keeping: the illustration is displayed SQUARE, not at its source 3:4. A 3:4 frame in the 482px column is ~640px tall and left the image hanging 130-150px below the last question whenever answers were collapsed; square keeps the left column short enough that 11 rows can fill it. The left column is deliberately the one that SETS the height (title + copy + square image = ~717px at any width, since `max-w-7xl` fixes the column at 482px); the panel follows it. No magic numbers — change the question count or the image and the columns still end on the same line. A sticky left column was tried and removed — with the heights matched its travel is 0. If questions ever pass ~14 the accordion outgrows the image and `lg:sticky lg:top-24` should come back. Illustration is desktop-only so it can't push the questions below the fold on a phone. FAQPage schema is untouched (still generated from the shared `FAQ` constant in faq-data.ts).
- 2026-07-26 INGEST — Refreshed the SEO baseline from the owner's Jul 26 GSC/Bing exports. The "3 month" GSC file is only 28 active days (Jun 26–Jul 23): 18 clicks / 1,556 impressions; Lithuania supplies 15 clicks while US+UK supply 1,014 impressions and no clicks. Two visible-query clusters still dominate (conversational AI vs chatbot: 516 impressions @ weighted pos 83.77; Zendesk: 474 @ 55.44). Current content is too young for a verdict: 27/58 posts are dated Jul 15+, including four Jul 25 posts absent from the export. Bing web has only 15 impressions/0 clicks, but AI Performance already reports 85 citations across 14/26 days, all sampled grounding queries centered on Zendesk research. Keep Jul 28 and Aug 18 checkpoints; next export page-filtered queries and Bing cited pages before changing targets. See seo-baselines.
- 2026-07-27 FIX — Quick-action clicks were auto-blocking innocent visitors as `prompt_attack`: the widget's envelope text `[Visitor clicked "…" — internal instruction, never quote…]` matched the CAPABILITY_PROBE regex (`internal…instruction`), and since every stored click in history matched too, the 3-prior-probes "sustained_probe_sequence" rule fired on the owner's own test visitor (5 of their last 8 messages were clicks). Fix in the detector, not the wrapper wording (history keeps old envelopes forever): `stripQuickActionEnvelope` reduces the envelope to its visitor-controllable label + prompt before assessment, applied to both the current message and history. Verified by replaying all three real visitors' prod histories: the false-positive visitor never blocks; the two reviewed abusers still block (directed_abuse at "retard fox", prompt_attack on the "Complete the sentence" probe sequence). 2 regression tests. See visitor-abuse-protection.
- 2026-07-27 FEAT — Moderation is now two-tier: kept the deterministic regex tripwires (tier 1, instant/free) and added a dynamic model review (tier 2, lib/ai/abuse-intel.ts): gpt-4o-mini judges the visitor's recent turns (quick-action envelopes stripped) for sustained bad-faith patterns — trolling/no-genuine-intent gets the new `bad_faith` reason (migration 20260727093000 widened the visitor_blocks reason check; applied to prod, and the July 23 migration-history drift was repaired so `db push` works again). The route fires the review in the background after tier 1 passes (void pattern, zero added latency); a verdict lands as an active block before the next turn, with detector 'llm-v1' + model rationale in the audit details. Prompt is deliberately block-averse (single weird messages, criticism, any-language genuine questions never block) and the module fails safe on model errors. Live-replayed on prod histories: false-positive visitor clean, troll blocked at turn 6 (bad_faith), prompt-prober at turn 4 — 5 turns earlier than regex. Vitest gotcha found: `beforeEach(() => mock.mockReset())` returns the mock, which vitest calls as TEARDOWN → phantom mock invocation; always brace hook bodies. See visitor-abuse-protection.
- 2026-07-27 FEAT — System prompts are now versioned (migration 20260727130000, applied to prod). `system_prompts.content` is the draft; Publish freezes an immutable `system_prompt_versions` row (v1..vN + note). The edit-repushes-to-every-bot loop in updateSystemPrompt is DELETED — bots pin `config.systemPromptVersionId` (live) and `config.previewSystemPromptVersionId` (test chat), and nothing changes for a bot until someone switches its dropdown. Key mechanic: both save actions re-snapshot the pinned version's content into `config.systemPrompt` server-side (`resolvePromptVersionSnapshot`), so the runtime chat path is untouched and clients apply versions without ever seeing prompt text; `/api/preview/chat` swaps in the preview version validated against the SAVED bot's family. Client UI = "Assistant version" section in ConfigForm (metadata-only dropdowns via `listAssistantVersions`); owner UI = publish/history in /owner/prompts (sidebar: Versioning → System prompts) + version dropdowns in SystemPromptSelect. Backfill pinned 7/8 linked bots to v1; 3IMIS stayed unpinned because its snapshot had drifted from the library content — it shows as "unversioned snapshot" until repinned. See bot-config.
- 2026-07-27 FEAT — Widget component library (migration 20260727150000, applied to prod + backfilled to the old implicit gating so nothing turned off). Three layers: code registry (lib/widget-components/meta.ts + registry.tsx previews; product-cards got a real second variant "compact"), provider folders in provider_components (folders derive from the CommerceProvider union via a `satisfies` labels record — new provider = compile error until labeled, folder auto-appears), and per-bot config.components variant map. Enforcement NARROWS code capabilities: chat routes suppress product cards / drop the order tool when unassigned, widget-config strips roomVisualizer/leadCapture, MessageList picks the product-cards variant. Owner UI = /owner/components (Uiverse hover-folders → grid + right add-drawer); per-bot UI = Components page (client sidebar + OwnerBotTabs tab) with a rendered variant drawer. availability read FAILS OPEN so a DB hiccup can't strip live widgets. See widget-component-library.
- 2026-07-28 CHANGE — Component folder assignment is now VARIANT-level (migration 20260728080000, applied to prod; unique triple provider+component+variant). Owner adds specific variants to a folder ("Product cards — Compact rows"); ONLY assigned variants appear in the client's variant drawer, setComponentVariant validates against the folder, and widget-config sanitizes a bot's stale choice to the first assigned variant when the owner removes one. Backfill kept every existing row as 'default', so nothing changed for live bots; extra variants are opt-in per folder. Owner folder grid shows one card per assigned variant; add-drawer lists unassigned variants. availability API is now assignedComponentVariants → Map<key, Set<variant>> (assignedComponents derives the key set; still fails open). See widget-component-library.
- 2026-07-28 FEAT — Three new component variants + real preview imagery. order-status "timeline" (progress stepper Ordered→Processing→Shipped→Delivered; non-linear statuses like cancelled skip it), lead-form "minimal" (frameless pill inputs, placeholder labels), room-visualizer "subtle" (white tray, accent button). Each is a variant prop on the widget component, wired through config.components in MessageList (order-status) and ChatWindow (lead-form, room-visualizer). Preview sample products now use real gpt-image-2 product shots (public/component-previews/{sofa,chair,table}.webp, generated by scripts/gen-component-previews.mjs) instead of the blog illustration. NB: new variants are invisible to clients until added to a provider folder in /owner/components.
- 2026-07-28 FEAT — TravelLine hospitality provider (spec docs/superpowers/specs/2026-07-28-travelline-provider-design.md). 'travelline' joined the CommerceProvider union; rooms map onto the existing CommerceProduct pipeline. Transport lib/commerce/travelline.ts (fixed partner.tlintegration.com host — no tenant-URL SSRF surface): OAuth client-credentials token cache (15-min TTL), Content API property cache (10 min), dated room-stay search, dateless room-type browsing, content-based details, validate. New chat tool check_availability (travelline only): ISO dates + adults + childAges → live offers with stay totals and TL's OWN bookingFormLink deep link (checkout/payment stays on TravelLine — their Search API terms require it). search_products = room TYPES from content, price-less by design; prompt guidance forbids quoting prices not from check_availability. No semantic index/catalog sync (a hotel has ~5-20 room types). Config: tlClientId/tlClientSecret/tlPropertyId (+optional hotel website in storeUrl), Test connection validates token+property. Components folder 'TravelLine' seeded with product-cards:default (migration 20260728100000, applied to prod). Unit tests with injected fetch (token reuse, mapping, fail-closed validate). LIVE TESTING still needs real TL credentials — hotel's TL account or partner test env. See commerce-connectors memory.
- 2026-07-28 CHANGE — TravelLine supports hotel CHAINS: tlPropertyId accepts comma/space-separated property ids (a "property" = one hotel on TL). One id → single-property GET (rich DetailedRoomStay: availability count, cancellation policy); many ids → multi-property POST /properties/room-stays/search (ShortRoomStay: NO availability/cancellation fields — treat offers as bookable, and say nothing about cancellation rather than falsely claiming non-refundable). Multi-hotel: titles prefixed with hotel name, room refs become propertyId:roomTypeId, details show "Hotel: X", prompt guidance tells the model to filter by hotel when the guest names one. Also: Configure screen sections now ALL start collapsed (removed Display's defaultOpen).
- 2026-07-28 FEAT — product-cards third variant "overlay": full-bleed photo card (object-cover, 240px), title on a bottom gradient scrim, price as a brand-colored badge top-right, out-of-stock top-left (+grayscale), and the WHOLE card is the tap target (no View more button). Same horizontal snap row as default; no visualizer toggle (single-action card). MessageList's variant pick is now the narrowing helper productCardsVariant().
- 2026-07-28 FEAT — quick-actions joins the component library: the welcome screen's suggested-action buttons extracted into the base component components/widget/QuickActionButtons.tsx (self-contained motion so it renders standalone in library previews) with three variants — default "Tiles" (unchanged 2-col glow tiles), "Pills" (centered chip wrap), "List" (stacked menu rows with chevron). Core folder, seeded with default (migration 20260728120000, applied to prod); widget-config strips suggestedQuestions per language when unassigned. WelcomeScreen delegates; ChatWindow narrows config.components['quick-actions'] via quickActionsVariant().
- 2026-07-29 FEAT — Sales Leads outreach lifecycle now includes `follow_up_email` plus a dedicated `status_updated_at` timestamp, shown as a relative age immediately after Status on desktop and beside Status on mobile/details. Company is narrower and Chatbot left the desktop grid while remaining available in qualification filters and details. Migration 20260729105857 was applied live: all 241 prepared bodies now end with the exact Lithuanian polite opt-out (idempotently), all 241 status timestamps are populated, and future email rewrites retain the sentence without corrupting lifecycle dates. See sales-leads.
- 2026-07-29 DATA — Expanded the live Sales Leads pipeline from 241 to exactly 300 with 59 first-party-verified Lithuanian prospects. Every new row has a canonical working website, public business email, platform result where detectable, personalized subject/body, exact opt-out, `has_chatbot=false`, and `ready` status; normalized-host/email guards prevent overlap with the existing pipeline. Added `scripts/research-sales-leads.mjs` for repeatable homepage/contact/about verification and platform/chat detection. See sales-leads.
- 2026-07-30 FIX/OUTREACH — Restored the standard `Laba diena,` salutation to all 59 July 29 researched drafts without touching their lifecycle timestamps, then sent the next 10 score-first Ready emails from `hello@loqara.com`. All 10 exact recipient/subject pairs were confirmed in Hostinger Sent before their rows moved conditionally from `ready` to `email_sent`. See sales-leads.
- 2026-07-31 OUTREACH — Sent the next 10 score-first Ready emails from `hello@loqara.com`: DekoSofa, Dviračiai internetu, Inuti, Kepsninės.lt, Kondikas, Kūdikių prekių išparduotuvė, LHP, Plumbera, Profkosmetika, and Termo1. All exact recipient/subject pairs were confirmed in Hostinger Sent before the rows moved conditionally to `email_sent`. See sales-leads.
- 2026-07-30 INGEST — Created the Loqara Meta app with Messenger + Instagram use cases and mapped its exact production gaps: Messenger webhook/test Page + `pages_read_engagement`; Instagram Login permissions/tester/webhook/business-login setup; real privacy/terms/data-deletion URLs; Loqara Business Portfolio, verification, Tech Provider/access verification, Advanced Access/App Review, and publish. The product still has only a disabled Channels card and implementation plan—no channel backend/routes yet. Three old Meta portfolios are scheduled for deletion; retry Loqara portfolio creation after 24 hours. See messaging-channels.
- 2026-07-29 FIX — Catalog sync died on big WooCommerce stores (maistassportui.lt) with "product_embeddings upsert failed: canceling statement due to statement timeout": product_embeddings has an HNSW vector index, so each inserted 1536-dim row pays a graph-insert, and a fixed 100-row upsert batch can exceed Supabase's statement_timeout (57014). Fix in the ONE shared path all providers use (lib/products/sync.ts): runChunkedWrite() starts at 50 rows and HALVES the batch + retries the same position on timeout (down to 1 row; a single-row timeout is a real error; non-timeout errors throw immediately). The unchanged-rows synced_at bump uses the same runner. 5 unit tests. Applies to woocommerce/shopify/magento/verskis alike — feed doesn't sync.
- 2026-07-29 FEAT — Widget chrome upgrades. (1) "Powered by" badge now shows the Loqara logo (widget.js + TestChat preview). (2) Expandable widget: theme.expandButton toggle adds a header widen/shrink button; ChatWindow posts cbz-expand → widget.js animates wrapper width +20% (504px) via a width transition — no reload; TestChat mirrors it. (3) "Talk with Agent" moved OUT of the header to a pill NEXT TO THE LAUNCHER (launcher height, dynamic width, compact/color settings respected): widget.js renders it, shows on open / dismisses on close with a slide-scale animation, and drives the in-iframe voice session via cbz-start-call/cbz-end-call; ChatWindow posts cbz-call-state back (in-call = red End pill). callButtonPlacement enum gains 'launcher' (new default; legacy 'header' normalized to launcher in publicBotConfig + preview builder); VoiceCallButton header appearance retired (always headless mount). Verified live on the landing bot: pill animates in next to the launcher, dismisses on close. Preview parity in TestChat via window-level postMessage (window.parent === window).
- 2026-07-29 POLISH — Widget header + call pill refinements: widen toggle moved to be the LAST header button (top-right corner); restart icon is now RefreshCcw at size-3.5 (visually balanced against the chevrons); language trigger shows the CODE ("EN"/"LT", 11px bold) instead of an emoji flag (illegible on colored headers) — the dropdown keeps flag + native name; the launcher-side call pill now rises from below (translateY 18px→0) with a 0.3s entrance delay so the panel lands first — delay is entrance-only in both widget.js (show timer, cancelled on hide) and TestChat (delay scoped to the animate transition, exit immediate).
- 2026-07-29 FIX/FEAT — Powered-by badge rework + call-pill race. (1) Badge hiding is now an OPT-IN choice: theme.hideBadge toggle in Configure ("Hide Loqara badge"), honored only when the plan has removeBadge (internal orgs always may); a paid plan alone no longer auto-hides — so the owner's own bot shows the badge again. (2) No-flash: widget.js badge starts hidden until a config (fetched or cached — hideBadge now cached in THEME_CACHE_KEY) says it's allowed. (3) Legibility on any host background: the badge is a translucent WHITE PILL (blur + shadow) instead of bare grey text — no background sampling needed; TestChat preview matches. (4) Race fix: opening the widget before /api/widget-config resolves used to skip the launcher call pill until a close/reopen — the config .then() now re-runs showCallButton() when already open. Tests updated (plan-gating semantics + loader badge markup).
- 2026-07-29 CHANGE — Powered-by badge moved INSIDE the widget: rendered by ChatWindow under the composer, centered, with text color derived from the configured chat background (dark chat → light text) — always legible, no host-page dependence, replaces the short-lived white pill on the host page. widget.js no longer renders any badge (loader test flipped to assert absence); TestChat preview footer removed (ChatWindow covers it); preview builder passes hideBadge from theme. Gating unchanged: theme.hideBadge opt-in × plan removeBadge (internal orgs exempt).
- 2026-07-29 FIX — Mobile had NO call button: the full-screen sheet hides the launcher, and the launcher-side pill (widget.js) is skipped on mobile, so "Talk with Agent" was unreachable. ChatWindow now renders it in the HEADER on mobile only (`callInHeader = voice && showCallButton && !callInComposer && isMobile` → VoiceCallButton appearance 'compact', shortLabel "Call Agent", respects theme.compactCallButton); desktop keeps the launcher pill, composer placement unchanged, and every non-header case still mounts headlessly since that component owns the voice session. Verified at 375px (header: ✕ · name · Call Agent · EN · restart; expand hidden) and that rotating to desktop swaps pill/header correctly — NB the CDP viewport override does not dispatch a `resize` event, so automated checks need a manual dispatch; real resizes fire it. Also: TestChat now passes isMobileOverride={false} — the preview is a fixed 420px DESKTOP card, so it must not inherit the config screen's width (which showed a header ✕ + a duplicate call button on narrow browsers).
- 2026-07-30 INGEST — Meta DevTools MCP connected (`claude mcp add --transport http meta_developer_tools https://mcp.facebook.com/devtools`; OAuth per restart; webhook manage is its only write). Audited Loqara app 2452581401910162: dev_mode, App Review NO_SUBMISSION, compliant, zero webhook subscriptions, contact email unverified, all legal URLs null. See messaging-channels.
- 2026-07-30 INGEST — Messenger spike LIVE: webhook route deployed, app-level page topic subscribed via DevTools MCP (handshake proven), signature path verified against prod (200/401), test Page connected + subscribed_apps. Next: real-message test, then persistence + shared processor. See messaging-channels.
- 2026-07-30 INGEST — Messenger AI replies live end-to-end: channel tables migration pushed, Loqara Page seeded as active connection to the Loqara bot, webhook persists + dedupes + answers via new lib/channels/processor.ts (widget grounding pipeline, non-streaming). Verified: real question got a KB-grounded answer in Messenger; event/conversation/messages/contact rows all correct. See messaging-channels.
- 2026-07-31 FEAT — Sales-leads screen overhaul. (1) STATUSES: 'accepted' renamed to 'wants_demo' and the demo track added — wants_demo → demo_ready → demo_presented → testing_bot → client (migration 20260731090000, applied to prod: 5 accepted rows became wants_demo). Each stage owns a hue so progress reads at a glance: grey ready → amber email_sent → sky follow_up → violet wants_demo → blue demo_ready → teal demo_presented → emerald testing_bot → accent client · red rejected. (2) STATS: four Demos-style cards (rounded surface + tinted corner glow + icon tile) showing Total leads (with open/switch split), Emails sent (contacted = every status past 'ready'; detail surfaces the follow-up-due count), Ready to contact, Switch opportunities. (3) FOLLOW-UP SIGNAL: `needsFollowUp()` in lib/sales-leads.ts (FOLLOW_UP_AFTER_DAYS = 14) — a lead STILL on email_sent past 14 days tints its whole row faint amber + shows a "Follow up" hint; a nudge or any progress clears it (8 prod leads currently qualify). 8 unit tests. (4) FILTERS simplified: quick tab strip (All / Ready / Wants demo / Emails sent, where the last groups email_sent + follow_up_email via StatusFilter 'awaiting_reply'), verticals moved from a horizontal scroller into a dropdown, chatbot-status dropdown dropped; the full stage dropdown stays for the demo track/clients/rejections and shares one state with the tabs. (5) 'Our client' rows render the ENTIRE line in the accent color — sub-components take an `onAccent` prop (ScoreTile, PlatformBadge, new VerticalBadge, CopyButton, StatusSelect, StatusAge) so badges/borders/muted text switch to translucent white instead of vanishing; same treatment on the mobile card list. NB owner screens can't be screenshot-verified from the agent (no session) — typecheck + 591 tests + prod data checks only.
- 2026-07-31 INGEST — Meta production path unblocked: Loqara Business portfolio (1577119393903065) created + Page added (user), app attached to portfolio and Tech Provider designation accepted (irreversible; adds business/access verification + App Review gates), pages_read_engagement added (all 5 Messenger perms ready for testing). Business Verification wizard handed to owner (documents). Driven via Claude-in-Chrome; devtools MCP now loads natively in-session. See messaging-channels.
- 2026-07-31 FEAT — Inbox ⇄ Messenger closed loop: shared deliverAgentMessage (send-to-Meta-first, persist-on-success; 24h-window + token errors → friendly toast, draft restored), processor now escalates handoff intent so Messenger convs reach the Inbox, resolved→bot reset in webhook, channel badges. Both inbox surfaces deduped onto the shared function. See messaging-channels.
- 2026-08-01 FEAT — Added per-bot Demand Radar: a read-only, schema-free aggregation of fallback/low-score/negative-feedback/missed-product conversations into product gaps, knowledge gaps, and store limitations; responsive trend/table/evidence UI; seven merchant action suggestions in an approval dialog; and intentionally disabled store publishing until provider write-back exists. Added desktop/mobile navigation, PII-redacted evidence, unit coverage, a development preview, and visual QA artifacts. See demand-radar.
- 2026-08-01 FEAT/SECURITY — Made Demand Radar action plans real. Every approved plan now persists evidence, drafts, and per-action outcomes; FAQ approval creates and ingests grounded QA knowledge; synonym approval activates a bot-scoped Unicode-aware rewrite in the shared commerce search path. Product attributes/descriptions/collections/notifications remain explicit saved tasks, and store publishing remains disabled. Three migrations were applied live for plan/synonym tables, least-privilege grants, consolidated org/owner RLS, and FK indexes; live transaction + 8-test RLS verification passed. See demand-radar.
- 2026-08-01 DESIGN — Replaced the 16 current LinkedIn draft visuals with a cohesive Higgsfield GPT Image 2 editorial-illustration set: hand-cut paper, ink grain, bold silhouettes, topic-specific visual metaphors, and versioned 1200×628 WebPs. Added a resumable generator plus a dry-run-by-default live assignment script; production rows must switch only after the assets deploy. See linkedin-content.
- 2026-08-01 UX — Added a persistent Demand Radar “How it works” dialog. It explains how conversation signals become opportunities, that every action requires review, which actions apply inside Loqara, which only save team tasks, and why direct product/store editing is not available yet. The critical product-editing answer appears first; the remaining walkthrough scrolls independently above a fixed mobile action. See demand-radar.
- 2026-08-01 POLISH — Reworked Demand Radar guidance into a shorter 31rem / 82svh panel with a fixed footer, spacious scrolling body, concise three-step workflow, and distinct active/review/not-yet capability cards. The persistent help trigger now matches the 40px store selector in a semantic blue information treatment; empty-state Conversations and Knowledge actions now use consistent 44px button targets. See demand-radar.
- 2026-08-02 INGEST — Added `docs/POTENTIAL_FEATURES.md`, a product-differentiation backlog for online stores. Demand Radar and its shipped capabilities are checked as complete; fourteen widget, merchant-tool, and standalone-service concepts remain explicitly unchecked for future prioritization. The document is intentionally separate from the committed engineering roadmap. See the wiki index.
- 2026-08-02 FIX/FEAT — Color-constrained product search ("white sofa" no longer returns random-color sofas). (1) Migration 20260802100000 (applied to prod) promoted the Verskis field-aware attribute/color ranking into the provider-neutral `match_products` — it acts purely on the shared buildDoc format, so it was never store-specific; Woo/Shopify/Magento now rank main-color matches first (live Baldaila regression: "balta sofa" → Spalva: Balta first, then white-mixes). Follow-up migration 20260802110000 gates the count-dominant tiers on FULL query coverage after the raw promotion regressed cross-language queries (homebynb "scented candle" → "Candy" lip balms); partial prefix coincidences now fall back to fused vector order. homebynb recall eval 16/17 (the 1 fail is a stale "pledas" case — no blankets in the catalog). (2) `docToDetails` reorders the `Attributes:` line FIRST and caps at 600 (was 400) — long category/tag lines were pushing the color line past the cap on half of baldaila.lt's sofas, so the model literally could not verify color. (3) Woo `candidateDetailsLimit` 8→20. (4) Prompt protocol (chat step 4 + voice, hash v31-color-constraints): attribute constraints are HARD — verify before display; a multi-value color attribute or a "spalvų pasirinkimas" note counts as ORDERABLE in the requested color (tell the shopper); no exact match → only closest light/neutral shades, said explicitly. ⚠️ Color matching is same-language — the model must query in the catalog language. See commerce.
- 2026-08-02 FIX/FEAT — "baltos sofos" on Baldaila found only 3 sofas because the actually-white ones have NO color in store data (only fabric codes like `Medžiaga: Jaffray 01` — whiteness lives in the photo), and the RPC's main-color pattern matched `Porankių Spalva:` (armrest color) as the product color. Fixes: migration 20260802120000 anchors main-color extraction to a field boundary (neutral RPC); `aiColorEnrich` (lib/products/enrich.ts) classifies the product PHOTO with gpt-4o-mini at sync time for changed products lacking a color attribute, appending `Color: …` in the catalog language; `scripts/backfill-product-colors.mjs` backfills already-indexed rows (run on Baldaila). Vision prompt must anchor language on categories, not titles — SKU-style titles made it emit English colors. See commerce.
- 2026-08-02 FEAT — Near-shade fill for color-constrained searches (chat prompt step 4 + display_products description): when exact-color matches are fewer than the intended set, the agent runs 1-3 extra searches with simple words for the CLOSEST shades (white → kreminė/šviesiai pilka/smėlio; red → bordo/terakota/ruda) and displays exact matches first, near-shades after, labeled as close alternatives. Color adjacency is LLM knowledge — the vector index encodes too little of it to rely on (product-type signal dominates). Voice unchanged: it deliberately runs one search per request. See commerce.
- 2026-08-02 FEAT — product-cards gains a 'grid' variant (2×2 equal square photo tiles, each tile links to its own product page; price badge, bottom-scrim title, out-of-stock state) — ProductCards.tsx + meta.ts + registry.tsx preview (4th sample product added so the grid previews full) + MessageList variant pick, RTL render test. Assigned to the WooCommerce folder in prod; bots opt in via their Components tab. Also: when the agent RECOMMENDS a subset of already-shown cards ("which fits my brown curtains?"), the prompt now mandates display_products with those ids — recommendations render as cards, never as bold-name bullet lists.
- 2026-08-02 FIX — The Components tab 404'd on DEMO bots: OwnerBotTabs renders the tab for every owner bot context, but /owner/demos/[botId]/ never got a components page when the library shipped (only configure/knowledge/embed). Added the demo-scoped page mirroring the client twin (demo-org guard + OwnerBotTabs base + BotComponentsView); setComponentVariant already handles any bot via the owner service-role path. See widget-component-library.
- 2026-08-02 FEAT — Present stage (/present/[botId]) now shows the demo bot's OWN store as the backdrop so the pitch reads as "bot already installed": live iframe when the store allows framing; most stores send X-Frame-Options (baldaila.lt: SAMEORIGIN), so the usual path is a WordPress mShots screenshot backdrop (first view may show its "generating" placeholder — reload once; upgrade path = playwright capture at demo-setup). Embeddability checked server-side through assertPublicUrl (SSRF invariant); no store / nothing renderable → the original dotted stage. Header lockup is the CLIENT name with the Loqara fox+wordmark underneath (quiet attribution, was "LOQARA DEMO" over the name); the "Click the launcher" hint is gone. Over a backdrop the lockup becomes a translucent chip. See owner-dashboard.
- 2026-08-03 OUTREACH — Sent the next 10 score-first Ready emails from `hello@loqara.com`: Vonia.eu, Žvejo magija, Aštunkė, Domus Classica, Durų linija, Hyteris, Interio, Lenkijos Prekės, Terracota, and Durų rankenos. All exact recipient/subject pairs were confirmed in Hostinger Sent before the rows moved conditionally to `email_sent`. See sales-leads.
- 2026-08-03 FEAT — Demo → client bot transfer: Transfer button on Demos cards moves the bot row to the chosen client org (all children follow via bot_id FKs — catalog index, knowledge, voice agent; embed key keeps working), purges demo conversations/events/leads by default, enforces the target plan's bot limit, warns on missing voice add-on, and leaves a demo_transfers tombstone (migration 20260803090000, applied) rendered as a "Transferred to X · View bot" card. Kills the re-create-and-re-sync-for-the-client workflow. See owner-dashboard.
- 2026-08-03 POLISH — Configurator navigation now uses a solid primary expanded-section header with white icon/title/chevron, muted-white description, and a 4px white bottom rule; collapsed sections remain white. Voice and Product visualizer's local headers were normalized to that shared state, and the old mirrored gradient CSS was removed. See bot-config.
- 2026-08-03 POLISH — Client portal branding now places the uppercase organization name as a slim, width-matched wordmark under Loqara; top-level sidebar active/hover treatments are square and full-width. Organization Analytics keeps its shared title/subtitle but now layers four rollup KPIs, an accessible grouped bot comparison, conversion/after-hours rates, and a cleaner drill-down table with explicit no-activity handling. See client-portal.
- 2026-08-03 POLISH — Increased client sidebar row height and removed the desktop content surface's left gutter/radius, so full-width active and hover states meet the content view directly while the outer shell keeps its top, right, and bottom spacing. See client-portal.
- 2026-08-03 FIX — Subscription reconciliation no longer crashes on a Stripe `resource_missing` customer. The server narrowly recognizes “No such customer”, conditionally clears that cached organization pointer and stale billing mirrors, renders the Free/Inactive screen, and creates a replacement customer on the next checkout; unrelated Stripe errors still surface. See plans-and-entitlements.
- 2026-08-03 POLISH — Rebuilt Subscription add-on cards around a consistent shadcn Card header/content/footer anatomy: stronger status and icon hierarchy, clearer pricing, three benefit rows, 44px buttons, and pinned footers so every action aligns regardless of description length. Available add-ons retain the brand accent; future add-ons stay visibly secondary. See plans-and-entitlements.
- 2026-08-03 FEAT/SECURITY — Added revocable 24-hour public links for demo presentations (migration `20260803152521` applied live). Share tokens carry 256 bits of randomness, only SHA-256 digests are stored, replacement revokes earlier links, and the public resolver rechecks expiry/revocation plus the bot's current `is_demo` organization on every request; Present itself remains owner-only. See owner-dashboard.
- 2026-08-03 FIX — Removed the widget's white flash on first open and conversation reload: widget.js now paints the iframe container and passes validated accent/background hints to the server-rendered embed loader, while ChatWindow's visitor-block gate uses the configured background instead of hard-coded white. See widget-and-embed.
- 2026-08-03 FIX — The restart-conversation confirmation sheet now uses the configured chat background instead of hard-coded white, with readable theme-aware prompt and cancel-button colors on both dark and light widgets. See widget-and-embed.
