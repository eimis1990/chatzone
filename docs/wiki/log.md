# Wiki log

Append-only, chronological. One line per entry, newest at the bottom.
Format: `YYYY-MM-DD TAG — note`. Tags: `INGEST` (new/updated knowledge),
`LINT` (health check), `DECISION` (a choice + its why), `FIX` (bug learning).

- 2026-07-08 INGEST — Wiki created (Karpathy LLM-wiki pattern). Seeded architecture, conventions, gotchas, widget-and-embed, bot-config, plans-and-entitlements, languages-i18n, rag-and-knowledge, commerce, access-model, blog-and-seo, voice from this session + prior working notes. rag/commerce/voice marked ⚠️ verify.
- 2026-07-08 FIX — Widget "currently unavailable": embed iframe's same-origin config fetch (no Origin header) was rejected by a non-empty allowlist; first-party requests now bypass it (`lib/widget-auth.ts`). See gotchas.
- 2026-07-08 DECISION — Free tier = one language of the client's choice (not English-only). `maxLanguages` entitlement; content de-anchored from `content.en`. See languages-i18n.
- 2026-07-08 FIX — Voice agent replies were samey ("Žinoma!" + a canned line) because `buildAgentPrompt` gave that exact phrase as the product-reply example, which gpt-4o parroted. Rewrote the delivery + product guidance to demand varied, request-specific, consultative phrasing; bumped `agentConfigHash` marker to `v21` so agents re-sync. Gotcha captured in voice.md: the hash doesn't cover hardcoded voice-block text.
- 2026-07-08 FIX — Voice agent leaked a Russian word ("обрадует") into a Lithuanian reply (gpt-4o code-switching). Added a language-lock to `buildAgentPrompt` keyed to the bot's `languages[]` (reply only in available languages, never Russian/others, no mid-reply mixing, don't switch unless the customer does). Bumped hash to `v22`. ElevenLabs V3 auto-detect is an unconstrained separate layer — next lever if it recurs.
- 2026-07-08 FIX — Voice: one question produced 3 product carousels + a loosely-related jumble. Cause: each `search_products` voice call renders its own list (`ChatWindow.handleVoiceSearch`) and the gift prompt told the agent to search several categories. Fixed: voice prompt now runs ONE focused search per request + asks a clarifying question (hash `v23`); `handleVoiceSearch` merges a burst of searches into one list. Pre-existing setState-in-effect eslint errors in ChatWindow left untouched (out of scope).
- 2026-07-10 INGEST — 3 AEO/SEO blog posts added (voice how-to `add-voice-ai-to-online-store`, WISMO `where-is-my-order-ai`, `ai-customer-service-small-stores`), each with 2 generated images + added to llms.txt. Voice-led angle mirrors the new hero. Gotcha: a blog title starting with `"` must be single-quote-wrapped in frontmatter (`title: '"..."'`) — `parseFrontmatter` strips one leading + one trailing quote.
- 2026-07-09 INGEST — Guided resolution for knowledge-check findings: "Resolve" opens `LintResolveDialog` (pick the correct line / keep both) → `POST /api/knowledge/lint/resolve` applies model-proposed verbatim find/replace edits to the source(s), re-ingests, dismisses. Also: lint prompt is now date-aware (no more "2026 is future" false positive); SourceDrawer shows a markdown preview + Edit toggle.
- 2026-07-08 INGEST — Knowledge-check findings are now resolvable: each links to its source(s) + an AI `suggestedFix`; "Fix" opens the source editor, "Dismiss" persists to `knowledge_lint_dismissals` (migration 0038). `lint.ts` maps excerpt refs→sources + adds stable fingerprints; lint route tolerates the table being absent (safe to deploy pre-migration).
- 2026-07-08 LINT — Hardened rag-and-knowledge, commerce, voice against the code (removed ⚠️ verify seeds). Corrections worth knowing: in-chat TTS/STT are RETIRED — `publicBotConfig` hardcodes them off, only the live-call agent works (`lib/widget-config.ts:199-205`); order lookup works only for woocommerce + magento, and the chat "discount" is a static configured code, not a live coupon API (`lib/commerce/index.ts`); the `api/widget/{order,search,discount}` routes back the VOICE agent, not the text chat tools; RAG has a canonical-pages subsystem (`lib/ingestion/canonical.ts`) the notes missed. Residual ⚠️ verify: voice per-minute metering (none in-repo), RAG eval "10/10" is point-in-time.
- 2026-07-10 INGEST — Added opt-in proactive launcher greetings: per-language random variants, immediate/delayed display, once-per-session/every-page frequency, custom colors/font/radius, dismiss/open interactions, and a faithful deterministic configurator preview. The host-page loader keeps the chat iframe lazy and respects reduced motion. See bot-config and widget-and-embed.
- 2026-07-10 DECISION — Reorganized Appearance around progressive disclosure and visitor-facing tasks. Core colors are compact cards; hex editing moved into a color dialog; advanced colors are hidden until requested; shapes use dense scrubbers; launcher, greeting, and chat-surface settings are separate groups. See bot-config.
- 2026-07-10 DECISION — Extended the Appearance hierarchy across the configurator: every feature section now uses compact task-based cards, conditional details stay disclosed only when enabled, and mixed select/slider grids use external field titles for consistent alignment. See bot-config.
- 2026-07-10 DECISION — Refined configurator density: secondary field explanations moved to accessible info tooltips, language availability became a multi-select dropdown distinct from primary language/content editing, compact sliders now share select geometry, and expanded section surfaces match their headers. See bot-config.
- 2026-07-10 DECISION — Config sections now form a gapless stack with white, left-gradient headers over muted expanded bodies. Save became “Save & Publish” and remains disabled until config or bot-name state is dirty; successful publishing resets that baseline. See bot-config.
- 2026-07-10 DECISION — Section headers now own a single bottom divider and show their animated left gradient only while expanded (with reduced-motion fallback); collapsed headers stay plain white. Shortened Store and Allowed domains summaries and separated the two language selectors by 20px.
- 2026-07-10 DECISION — Color editing now keeps the floating widget preview sharp above the dialog's blurred backdrop and streams valid picker/hex changes live. Cancelling or dismissing restores the opening color; Apply commits it. Other dialogs retain normal full-screen modality.
- 2026-07-10 FIX — The first color-preview layering attempt still blurred the widget because its z-index remained trapped inside the configurator preview pane. The live widget now renders through a body-level portal, with explicit color-dialog layers (backdrop 40 → preview 45 → content 50).
- 2026-07-10 FIX — Live proactive greeting reused the launcher's 24px/2.5px-stroke close SVG, making its X larger than the preview's Lucide icon. Added a dedicated 16px/2px-stroke greeting X and a loader regression assertion.
- 2026-07-10 FIX — Landing-page widget teardown omitted the body-mounted proactive greeting, leaving it visible after navigation/login while the launcher disappeared. `WidgetEmbed` now removes the greeting with every other widget surface and has a lifecycle regression test.
- 2026-07-10 INGEST — Added `docs/CHANNELS_IMPLEMENTATION.md`: Messenger-first scope, subscription/connection UX, Meta setup, provider architecture, schema, billing, webhook, security, Inbox, testing, rollout, and definition of done.
- 2026-07-10 FIX — The widget loader's “Powered by Loqara” badge still targeted the retired `loqara.app` domain. It now uses the canonical `https://www.loqara.com` marketing URL, protected by a loader regression test.
- 2026-07-11 DECISION — Sales leads became a responsive CRM-style workspace with summary metrics, compact filters, mobile lead cards, and an accessible detail panel. Clipboard copy now contains only `email_body`; stored outreach emphasizes contextual Lithuanian conversation and the verified 1,500–12,000 paid-plan limits. See sales-leads.
- 2026-07-11 DECISION — Refined the sales-leads workspace into the owner UI's compact page hierarchy and a flatter score-first data grid. Platform is a dedicated column, city is detail-only, and status colors now encode the lead lifecycle. See sales-leads.
- 2026-07-11 FIX — Sales-lead details no longer collapse long cards or clip the prepared email: the drawer now has a flex-constrained scroll viewport, non-shrinking content sections, clickable website metadata, and an isolated Base UI starting/ending-state slide transition. See sales-leads.
- 2026-07-11 DECISION — LinkedIn planning is now a responsive, keyboard-accessible draggable board with persisted per-column order, 20 founder-led drafts, 10 distinct ideas, and 20 exact-text editorial visuals. Stable dnd-kit context IDs prevent SSR hydration mismatches. See linkedin-content.
- 2026-07-11 FIX — Owner dashboard LEADS card showed 5: not a code bug, just seeded Aurora Living demo leads (identical timestamps, fake names). Deleted the 5 demo rows; card now reads real 0. See owner-dashboard.
- 2026-07-11 FEAT — Added an MRR/earnings headline card to the owner dashboard (`MrrCard` + `computeMrr`, DB-only, no Stripe call). Live snapshot only — no MRR history stored yet, so a growth-over-time chart is deferred. See owner-dashboard.
- 2026-07-11 CHORE — Seed script (`scripts/seed-linkedin-content.mjs`) now defines the draft board order via an explicit `DRAFT_ORDER` id list (+ coverage guard) instead of array position, so a reseed restores the recommended founder-POV-first sequence. See linkedin-content.
- 2026-07-11 DECISION — Lint cleared to 0 errors: fixed 3 mechanical issues (unescaped entity, 2× prefer-const) and downgraded the 5 new React-Compiler `react-hooks` rules to `warn` (they fire on intentional, hydration-safe pre-compiler patterns). See gotchas.
- 2026-07-11 FEAT — Added per-draft LinkedIn hashtags (3–5 each) in `scripts/linkedin-hashtags.mjs`, appended to the 20 draft bodies for discoverability. Shared by the seed (build-time append, tag-free source so no double-append) and a one-off DB backfill. See linkedin-content.
- 2026-07-11 FEAT — Commerce agent is now aware of its own suggestion cards across turns: last shown products are read back from `messages.products`, injected into the prompt (numbered, display order) and seeded into a separate `shown` map so `display_products` can re-show by id. Verified end-to-end ("tell me about the first one" resolves; no phantom cards on non-product turns). See commerce.
- 2026-07-11 FEAT — Commerce agent can now discuss and compare products: semantic matches carry the indexed doc (categories/tags/attributes/desc, title-stripped, ≤400 chars) as `CommerceProduct.details`, returned to the model for the top 8 results per search. Verified end-to-end (meaningful two-candle comparison with scent-specific reasoning). See commerce.
- 2026-07-11 FEAT — `get_product_details` tool (WooCommerce only): live full description (≤1500 chars) + attribute lines for up to 3 ids via the public Store API, behind the SSRF guard. Also fixed the shown-cards prompt block to include ids — without them the model guessed ids cross-turn and details/re-display silently failed. Verified end-to-end (burn time, wax blend, fragrance notes answered from live data). See commerce.
- 2026-07-11 FEAT — Product details now work on Shopify (Storefront `nodes` query: full description + options-as-attributes) and on the VOICE agent for both Woo+Shopify: new `get_product_details` client tool (expects_response, name-based — voice LLM never sees ids) → `/api/widget/details` resolves the spoken name via searchCatalog and returns a spoken summary. Agent hash bumped to v24 so existing voice agents re-sync. See commerce.
- 2026-07-12 FIX — "Slim Lady" search returned soap dispensers; three root causes fixed: (1) migration `20260712060000` adds a diacritic-folded title-token boost to `match_products` (FTS 'simple' never matched "skaidulu" vs "skaidulų"); (2) prompts/tool descriptions now force verbatim brand-name searches; (3) `audience=unisex` is neutralized to no-filter in `searchCatalog` — the RPC filter was excluding women-tagged products when the model volunteered unisex. Also: chat route now logs each model search query; supabase CLI upgraded (brew, also bumped node 22→26 — tests fine) and migration history repaired (0018–0038 marked applied). See commerce.
- 2026-07-12 FIX — Card awareness now works in the configurator PREVIEW too: it's a separate stateless route (`/api/preview/chat`) that never got the /api/chat features — buildHistory dropped card-only messages entirely and sent no products. Client now carries assistant products in preview history; route derives shownProducts + shown map. Verified with an authed request (magic-link session, revoked after). Also clarified the "Smart product search" config copy (catalog index ≠ Knowledge screen). See commerce.
- 2026-07-12 FEAT — Smart product search UX: idle view shows "X / Y products indexed · last synced … · auto-resyncs nightly" (new GET /api/products/sync status endpoint), sync progress now has live numbers for every phase (fetching count, embedding batches), copy explains the nightly cron and the catalog-vs-Knowledge split. Found via the new display: a transient page failure silently truncated the index (1,600/2,582) — fetchWooCatalog now retries a failed page once. See commerce.
- 2026-07-12 FIX — Sync 504 post-mortem: the timed-out run WIPED the index down to 400/2,582 products because refresh was delete-then-insert; now upsert-then-prune (synced_at stamp as prune key) so timeouts can't destroy the index. Index repaired via localhost run. See gotchas.
- 2026-07-12 FIX — Lead capture "After N messages" was a dead option: the enum/UI existed but nothing counted messages (only on_fallback + manual worked). Now counted client-side in ChatWindow (visitor's Nth message opens the form); `afterNMessages` threaded through publicBotConfig (it was stripped). Enabling lead capture with zero fields now seeds Name + Email defaults — an empty fields list meant the form could never render.
- 2026-07-12 FEAT — Lead form is now localized + customizable: built-in EN/LT strings (title, Send, thanks, validation) follow the widget language, with an optional per-bot `leadCapture.title` override threaded through publicBotConfig. Trigger dropdown explains each option (fallback / Nth visitor message / manual quick action) with a persistent hint; enabling lead capture seeds Name+Email field defaults in the bot's primary language (Vardas / El. paštas for LT).
- 2026-07-12 FEAT — Incremental catalog sync: `product_embeddings.raw_hash` (migration 20260712190000) + diff in sync.ts — unchanged products skip AI enrichment and embedding, just a synced_at bump. Measured: 3IMIS (coffeeloft.lt, 1,480 products) first sync ~5 min, re-sync 59 s → re-syncs fit the Vercel 300 s budget. Also fixed the progress-poll race (stale 'done'/'error' row killed the display on every retry). 3IMIS indexed 0→1,480; second HomeByNB bot re-synced. See gotchas.
- 2026-07-13 FEAT — Launcher customization: 6 selectable icons (chat/bubble/dots/help/sparkles/headset) + close-icon choice (X / arrow-down) + bottom/side spacing sliders (8–120px, side follows position). SVGs live in `lib/launcher-icons.ts` with a synced copy in `public/widget.js` (static file, can't import — a unit test asserts every key exists in both). Offsets apply to the launcher, pulse rings, greeting bubble, and panel; verified live on the landing widget (sparkles + chevron + 48/72 offsets, then reverted).
- 2026-07-13 FEAT — Launcher icon set grown to 10 (added paper plane, lightning, smiley, shopping bag); spacing sliders tightened to 10–50px. The "icons invisible" report reproduced as a deploy/compile artifact — a dev probe page with the exact picker markup rendered all icons correctly (jsdom + real browser), so the picker code is sound; hard refresh after deploy.
- 2026-07-13 FIX+FEAT — The "invisible" launcher icon picker was white-on-white: the bot's launcher color is white and the picker hardcoded white icons (the live widget auto-contrasts; the picker didn't). Picker + TestChat preview now use `readableTextColor`, and a new `theme.launcherIconColor` lets owners set an explicit color for BOTH icons (empty = auto contrast) — threaded through publicBotConfig into widget.js.
- 2026-07-13 FIX — Voice agent finally "sees" products: `search_products` (and order/discount) had ElevenLabs-default `expects_response: false`, so the LLM never received the search summary — cards rendered but the agent didn't know what it found (couldn't name products, answer "the first one", or chain into get_product_details). All client tools now set `expects_response: true`; hash bumped v26 and all four voice agents force-resynced + verified via the ElevenLabs API. Also: launcher icon swaps (paper plane→robot, question→smiley bubble).
- 2026-07-13 FIX — "Deja, neturiu išsamesnės informacijos" while the store page had a full description: the model judged the 400-char search `details` as all it had and never called get_product_details (its description was too soft — "use when search details don't cover it"). Now a hard rule in the tool description + a PRODUCT DETAILS prompt block (chat, gated on productDetailsSupported) + the voice prompt: MUST call before answering "tell me more", NEVER claim lack of info without having called it. Voice hash v27, agents resynced. E2E: TIRTIR "papasakok daugiau apie pirmą" → full ingredient breakdown.
- 2026-07-13 FIX — Voice details in the configurator preview: the preview transport's get_product_details was a stub ("not available in the preview"), so playground voice calls apologized while the live widget worked. New authed /api/preview/details mirrors /api/widget/details (name → searchCatalog top-1 → full description); transport wired. Same preview-is-a-separate-route trap as card awareness — see commerce.
- 2026-07-13 FIX — Sales leads: copyable email/phone in the detail panel + a subject copy button (subject pattern "Company: idėja svetainei" kept — short, personalized, non-salesy). Audited all 231 prepared emails with a strict LLM pass: 18 openings were unfinished fragments ("Jūsų salonai Kaune ir Klaipėdoje plius e-parduotuvė."), minimally repaired into complete sentences keeping every personalized fact, business-specific connectors, no stock phrases (dry-run reviewed before applying).
- 2026-07-13 GOTCHA — dropslietuva.com "Test connection" fails only from production: Cloudflare blocks Vercel egress at the IP/ASN level (localhost + curl pass; browser-like headers don't help — added STOREFRONT_HEADERS to all Woo storefront fetches anyway for the header-scoring case). Client-side fix: WAF exception for /wp-json/wc/store/*. See commerce.
- 2026-07-13 FEAT — Demo bots in the owner portal: /owner/demos lists/creates showcase bots in a find-or-create "Loqara Demos" org (organizations.is_demo, migration 20260713120000; enterprise+voice so the editor exposes everything). Editing reuses the done-for-you configure route; /present/[botId] is a clean full-screen stage (bg-dots + real WidgetEmbed) for screen-sharing pitches. owner_stats now excludes platform AND demo orgs (CLIENTS 7→6, conversations 104→95 — platform-bot traffic no longer counted); dashboard recent-activity + clients list filter both.
- 2026-07-13 FEAT — Demos section grew a full editor + duplication: /owner/demos/[botId]/{configure,knowledge,embed} mirror the done-for-you editor (OwnerBotTabs gained a `base` prop; violet "Editing demo" banner) so editing a demo never routes through /owner/clients. "Create from demo" on the client detail page duplicates a demo into the client org via lib/bots/duplicate.ts — config + knowledge sources/chunks (embeddings, ids remapped) + the whole product index (skips the first sync); source must be in the is_demo org, plan bot-limit enforced; verified against the real DB. Aurora Living demo account deleted (scripts/demo-cleanup.mjs) — stats now 5 clients / 52 conversations. vitest now stubs `server-only`.
- 2026-07-13 FEAT — Demos screen redesigned: brand-glow cards with avatar/monogram, store host, indexed-products badge ("605 products indexed" vs "Catalog not synced"), created date; polished New-demo panel. A permanent collapsible "Store blocks the connection? (Cloudflare)" reference embeds the client-facing WAF skip-rule instructions (URI starts with /wp-json/wc/store/ → Skip Bot Fight Mode) + the call script, so the owner never needs to re-derive it.
- 2026-07-13 FIX — Cloudflare challenge pages were being INDEXED as knowledge (dropslietuva crawl: 18/18 sources were "Performing security verification" interstitials marked Ready). `looksLikeBotChallenge` in parse.ts rejects challenge output from Jina (falls back to direct fetch, which passes from residential IPs) and errors the source when direct fetch is challenged too. Junk sources deleted; re-parse of the same URL returns real content. Also: Toaster pinned to max z-index so toasts render above the widget preview/launcher.
- 2026-07-13 FIX — Toasts genuinely above the widget preview now: the preview PORTALS to document.body (z-45) while the Toaster sat inside the layouts' `isolate` stacking context — no z-index could cross that boundary; Toaster moved outside the isolate wrapper in both owner and client layouts. Voice ASR LT→LV drift mitigated via `asr.keywords` biasing (no hard pin exists — asr.language is silently dropped); agents resynced at v28. See voice.
- 2026-07-15 FEAT — Header styles + header toggles: `theme.headerStyle` ('classic' | 'floating' inset pill | 'curved' body-card overlap), `theme.hideHeaderLogo`, `theme.compactCallButton` (icon-only square call button via VoiceCallButton's new `iconOnly` prop). Picker ("Choose a style") sits above the theme presets in Appearance; toggles live in Chat surface. New theme keys must be added in FIVE places or types break: lib/types.ts, lib/widget-config.ts (type + publicBotConfig), lib/validation/schemas.ts (field + default literal), lib/widget-theme-presets.ts (`base` + PRESET_PRESERVED_THEME_KEYS for layout/feature keys presets must not clobber), TestChat.tsx buildPreviewPublicConfig.
- 2026-07-15 BLOG — Added a three-post commerce-discovery cluster:
  `agentic-commerce-ecommerce`, `how-to-get-products-recommended-by-chatgpt`,
  and `ai-product-recommendation-chatbot`. Each has a 40–60-word Quick Answer,
  seven FAQ entries for schema, primary-source citations, internal cluster
  links, and an OpenAI-generated 1200×800 WebP hero. Added all three to the
  manually curated `public/llms.txt` guide list.
- 2026-07-15 DECISION — Rewrote all 231 sales-lead emails around a two-sentence relevance hook (verified observation → concrete website task), removed first-touch plan-limit copy, and split the shared pitch into e-commerce/catalog and service/organization variants. Unsupported buyer-behavior claims are rejected; 19 incumbent-chatbot leads acknowledge the existing assistant respectfully. Added a validated dry-run/repair/apply script. See sales-leads.
- 2026-07-15 FEAT — Configurator section animations: CollapsibleSection now uses framer-motion for smooth ease-in-out expand/collapse (height+opacity via AnimatePresence variants) with `staggerChildren` that propagates through MotionContext to SettingsGroup motion items (they inherit variant names, no own initial/animate → descend-into-place fade). overflow is hidden during the height animation, flipped to visible onAnimationComplete so Selects/popovers/tooltips aren't clipped. Section header gradient now glows from BOTH edges (`::before` left + `::after` right) and open sections get a 2px bottom border. reducedMotion="user" strips the transform descend. Gotcha logged: grouped `::before,::after` rules drop the shared block under Lightning CSS — see gotchas.md.
- 2026-07-15 BLOG — Added five non-cannibalising commerce guides:
  Google AI Mode shopping, semantic e-commerce search, conversational commerce,
  Magento AI-chatbot setup, and safe returns automation. Each includes a
  40–60-word Quick Answer, seven FAQ entries, internal links, primary evidence,
  and a text-free OpenAI-generated 1200×800 WebP hero; all five were added to
  `public/llms.txt`.
- 2026-07-15 FIX — Configurator section animation polish: (1) Collapse is now INSTANT (removed framer `exit`) — an animated height collapse shrinks the scroll content frame-by-frame, making the ResizablePanel clamp scrollTop and the sticky `backdrop-blur` toolbar repaint every frame = visible flicker/"refresh" when collapsing while scrolled. Verified: scrollTop stays put + no mid-height body on collapse. Expand still animates. Do NOT re-add `exit` to CollapsibleSection. (2) Card descend slowed to be noticeable: SettingsGroup item duration 0.3→0.45, y −8→−14; body staggerChildren 0.05→0.09, delayChildren 0.06→0.08, duration 0.3→0.36.
- 2026-07-15 TWEAK — Section accent border moved: the 2px border now sits under the section HEADER (with the glow) in the primary/accent color (`border-b-2 border-primary` on the open header), not at the section's bottom. Closed headers keep the neutral 1px `border-b`.
- 2026-07-15 FIX — Blog Markdown tables no longer rely on horizontal scrolling in the 42rem article column. `lib/blog.ts` adds column-count/layout metadata plus a `data-label` per cell; container-query CSS presents 4–6-column comparisons as compact labelled cards and stacks 3-column tables on phones. Verified at 320/375/414/768/1024/1440px with zero page or table overflow. See blog-and-seo.
- 2026-07-15 FEAT — LinkedIn planning gained 10 broader CX, operations, knowledge, trust, commerce, and accessibility drafts with a cohesive OpenAI-generated 1200×628 WebP visual series. Board cards now show only image/title/suggested order/drag handle; clicking opens a full-height right drawer with complete copy, metadata, copy actions, pipeline status, delete, and save. The idempotent `--new-only` seed mode puts the new set at Suggested #1–10 while retaining existing draft order and statuses. See linkedin-content.
- 2026-07-15 FEAT — Chat typography expanded from 6 to 24 free, open-source Google Fonts grouped into business, playful, serif, and monospace sections, including Baloo 2, DynaPuff, Mali, and Atma. The font picker is now a compact transactional dialog: it remains open while choices preview in the live chat, Keep accepts, and Cancel or dismiss restores the opening font. The redundant pangram was replaced by a 400–700 weight slider. `theme.fontWeight` is validated, saved, previewed, exposed in the public widget config, and applied at the ChatWindow root; visual theme presets preserve it. Optional font families are self-hosted by `next/font` without global preloading. See bot-config.
- 2026-07-17 BLOG — Added six intent-separated guides in two clusters: e-commerce GEO / IndexNow / AI-search measurement and knowledge-base preparation / hallucination prevention / pre-launch chatbot testing. Every article includes a 40–60-word Quick Answer, seven FAQ entries, primary evidence, deliberate cross-links, and an original text-free 1200×800 WebP hero. Added all six to the curated `public/llms.txt` list. See blog-and-seo.
- 2026-07-16 FIX — Quick-action prompt leak: when a quick action's hidden `prompt` differs from its visible `label`, the widget now wraps the sent message as `[Visitor clicked "<label>" — internal instruction, never quote or mention it: …]` (ChatWindow handleQuickAction) and buildSystemPrompt gained a QUICK ACTIONS rule: reply in terms of the label only, never reveal instruction text/URLs/slugs — including in failure apologies. Trade-off: the wrapper slightly pollutes the RAG retrieval embedding for these messages; the weak-retrieval rewrite path covers it.
- 2026-07-16 FEAT — "Show products" quick action (deterministic) + list-products-by-URL: new quick-action `action: 'products'` with a `query` field (search phrase OR store category/tag/collection page URL) — click renders product cards directly via /api/widget/search, no LLM round-trip (ChatWindow runProductsAction; localized note/empty copy). URL support lives at the shared point `searchCatalog` (lib/products/search.ts): an http(s) query first tries `listStoreProductsByUrl` (lib/commerce/index.ts) — Woo resolves the LAST path segment as a term slug (Store API categories list → `?category=ID`; tag via public `wp/v2/product_tag?slug=` → `?tag=ID`; permalink bases vary per language so never match on them), Shopify matches `/collections/<handle>` via Storefront collection query; magento/feed return [] and the slug words ("natalijos-hitas" → "natalijos hitas") become the search query. Because it's in searchCatalog, the LLM search_products tool + voice + preview all gained URL support too. Verified live against homebynb.lt (tag 3103 → 24 products) + unit tests with injected fetch.
- 2026-07-16 TWEAK — Product cards narrowed 80%→72% (ProductCards.tsx, more next-card peek). Fixed curved-header "See all" list view: the body's rounded top corners showed through as notches above the list overlay's pink sub-header bar. Fix: ChatWindow drops the body's curved top radius (`borderRadius: listProducts ? '0' : …`) while the full-screen list overlay is open — the overlay covers the whole body so the curve serves no purpose then. Classic/floating styles unaffected. Diagnosed via element tinting: the notch = whatever sits behind the body's rounded corner (header or root bg).
- 2026-07-16 FEAT — Mesh-gradient chat backgrounds: 8 presets (lib/widget-gradients.ts) as tiny inline SVG data-URLs (blurred circles over a base) stored in the EXISTING theme.backgroundImageUrl — zero schema/widget changes; opacity slider, LogoUpload preview/remove, and preset-clearing all apply as-is. Selecting also sets theme.backgroundColor to the preset base (pre-image paint + drives dark-body text contrast, so dark gradients get light text). Swatch grid sits above the background-image upload in Chat surface. GOTCHA: encodeURIComponent leaves ' ( ) literal, which breaks UNQUOTED CSS url(...) interpolation — data URLs must escape them (%27 %28 %29); ChatWindow's backgroundImage url() is now quoted too.
- 2026-07-16 TWEAK — Gradient presets expanded 8→20 (lib/widget-gradients.ts): +6 subtle lights (near-white bases, 2-3 gentle tints: mist/linen/pearl/mint/vanilla/cloud) and +6 deep darks (near-black bases, 2-3 muted glows: obsidian/abyss/nocturne/charcoal-ember/night-forest/black-plum). Configurator swatches switched from an 8-col grid to a horizontal scroll row (h-16 w-12 shrink-0, thin scrollbar).
- 2026-07-17 FEAT — Composer-placed call button + voice-mode visuals: new `theme.callButtonPlacement` ('header' default | 'composer'). Composer placement hides the header call button but still MOUNTS VoiceCallButton with new `appearance='none'` (display:contents wrapper so no phantom flex gap) — it owns the ElevenLabs session; onReady now exposes {start, end} (startVoiceRef/endVoiceRef in ChatWindow). Composer right slot: phone button (callButtonColor) when field empty → morphs into send while typing; radius follows the field radius (capped 19 = circle). Disabled dictation-mic placeholder sits left of it (composer placement only, no STT wired yet). During a call: composer becomes a waveform bar (.cbz-wave-bar, deterministic delays, data-speaking boosts amplitude) + red end pill; a brand-tinted SiriOrb (components/widget/SiriOrb.tsx) pins above the transcript (flex-shrink-0 strip; MessageList is flex-1 overflow-y-auto so bubbles scroll UP TO the orb) for the whole call — welcome screen is skipped while call active. Orb shows for calls in BOTH placements; wave bar only in composer placement. Configurator: "Call from message field" toggle (voice-enabled only); Compact toggle hidden when composer placement. Known gap: mic-denied/call errors are silent in composer placement (header mode showed inline text).
- 2026-07-17 FEAT — Voice polish trio: (1) composer-placed call errors now surface — VoiceCallButton `onIssue` emits codes (mic-denied/unavailable/error), ChatWindow maps to localized copy, Composer renders a notice line above the bar. (2) Whisper dictation, plan-gated: `dictation` entitlement (free=false, all paid=true; bullet in plans-catalog Starter cascade), PublicBotConfig.dictation, POST /api/widget/transcribe (origin+plan gate, 6-cap rate limit, 5MB cap) + /api/preview/transcribe (authed, same gate) → lib/ai/transcribe.ts (whisper-1 direct fetch, language hint). Composer mic: MediaRecorder (browser-default container: webm Chrome / mp4 Safari — Whisper takes both), 60s auto-stop, red pulsing while recording, spinner while transcribing, text appends to field. `transcribe` is OPTIONAL on ChatTransport; widget+preview implement it; button shows only when config.dictation && transport.transcribe. canUseDictation threaded ConfigForm→TestChat from all four configure pages (ent.dictation). widget.js iframe already allows microphone. (3) Call orb now sits in a brand-tinted halo (radial glow + 1px ring + outer shadow, intensities differ for dark bodies; orb bg pure white/#0d0d12 + 50% brand color mixes) so it reads on any background — the plain orb washed out on light themes.
- 2026-07-17 FIX — SiriOrb rebuilt on boring CSS: the reference implementation (registered @property angle animation + backdrop-filter + mix-blend-mode overlay + contrast() filter) rendered wildly differently across browsers — Chromium showed a living orb, Safari a static neon blob (angle animation frozen at 0° stacks all conic layers). New version: static layered radial-gradient base + a blurred conic layer spun with plain transform rotate + a soft top-light/vignette ::after. Renders identically everywhere, still looks like an orb with animations off (reduced motion). Same props/API. Preview and live widget were never different — it was the browser.
- 2026-07-17 TWEAK — SiriOrb v3: particle sphere on 2D canvas (900-dot fibonacci sphere, orthographic projection, transform spin) replacing the CSS-gradient orb — dim brand-dust globe + vivid turbulent rim (light tint top → deep shade bottom, all derived from the brand hex in JS; canvas can't use color-mix). New `activity` prop (0–1) drives spin speed, rim scatter, dot size and alpha with eased transitions — ChatWindow maps callState: speaking→1, connecting→0.55, listening→0.22, so the orb calms while the visitor talks and comes alive when the agent speaks. Halo wrapper removed (the particle rim self-defines). Reduced motion renders a single static frame. Dot radius scales with canvas px so any size works.
- 2026-07-17 TWEAK — Orb active state punchier (displacement 0.11→0.17, spin +0.55→+0.75, turbulence clock ×(1+level·0.9) so particles move further AND jitter faster while the agent speaks). Dictation recording now turns the message field into the same waveform-with-red-stop as live calls (waveField helper shared by both states in Composer; stop → Whisper transcribe → text appends). Verified end-to-end by stubbing getUserMedia with canvas.captureStream() in the browser.
- 2026-07-17 FIX — Orb "wild spinning" bug: scaling the turbulence clock by activity (`t = now * factor(level)`) made every particle's phase leap whenever the eased level changed (absolute time × changing factor). Clock and spin reverted to constants; activity now scales ONLY the displacement amplitude. Rule: never multiply absolute time by a state-dependent factor in an animation — integrate speed via dt instead (like rotY).
- 2026-07-17 FEAT — Added first-class Verskis commerce support after `mobel.lt` was incorrectly tested as WooCommerce: auto-detection switches and persists the provider, validation counts public product sitemaps, and live HTML search/category parsing produces current product cards without credentials. It initially launched keyword-only (semantic sync added later the same day); order lookup remains unsupported. Live-verified against mobel.lt (1,951 products). See commerce.
- 2026-07-17 FEAT/FIX — Added and published a specialized Furniture Store system prompt, assigned it to the Mobel and 3IMIS/Chafox Verskis bots, and hardened the Verskis data path it depends on. The prompt keeps a hard/preference/unknown constraint ledger across turns, performs strict unit/dimension reasoning, verifies color/material/orientation via details, and avoids global “no match” claims after attribute searches. Opaque product ids resolve through candidate/card URLs for full attributes, while parser boundaries exclude alternative/related-product attributes and `results_total` excludes recommendation carousels from search matches. Live-verified on mobel.lt product details. See commerce and bot-config.
- 2026-07-17 FIX — Mobel furniture recommendations now arrive on the first useful turn and render as cards instead of prose hyperlinks. Verskis search concurrently enriches up to 12 candidates with main-page attributes, so a base `sofa` search exposes exact `Spalva: Kapučino` on CA95931 without four serial detail batches; empty narrow searches return an explicit base-category retry instruction. Restored opaque numeric model ids (URLs had encouraged Markdown links), remapped them internally for details, made the furniture prompt search immediately instead of asking for unspecified dimensions, and streamed `display_products` cards into the UI before final prose completes. Browser-verified the exact Lithuanian request: TI28201 + CA95931 cards in ~2.7s, first turn. See commerce and bot-config.
- 2026-07-17 FEAT — Verskis joined the full Smart product search pipeline: Sync catalog is visible, product sitemap pages crawl with 16 workers into rich category/description/brand/attribute docs, embeddings/upsert-prune use the existing Supabase vector schema, semantic results hydrate live price/stock from stored product URLs, nightly cron includes already-indexed Verskis bots, and configurator preview now uses the same semantic `searchCatalog` path as the live widget. Verskis skips generic AI recipient tagging to keep first sync under Vercel's 300s cap; full Mobel source crawl live-verified at 1,951 unique products in ~69s. Stale cross-store index URLs fall back safely until sync replaces them. See commerce and gotchas.
- 2026-07-17 FIX — Product matching is now Lithuanian-inflection and field aware. Main product color outranks component color/general prose, so Mobel's 11 `Spalva: Balta` dining chairs are retrieved for `baltos kėdės`; canonical catalog-form queries were also added to the commerce/furniture prompts. See commerce and gotchas.
- 2026-07-17 DECISION — Provider isolation is now a hard commerce invariant. Added one commerce profile per provider (catalog fetch/enrichment policy, matcher RPC, capability check, hydrator, detail-reference conversion, guards, optional guidance), moved Verskis field-aware ranking to `match_products_verskis`, restored the neutral matcher for Woo/Shopify/Magento, and added isolation tests plus a dedicated extension checklist. Shared search, sync, and AI tools now orchestrate profiles instead of branching on a provider/store. See commerce-provider-profiles.
- 2026-07-17 FIX — Verskis broad browsing now returns up to 20 verified cards without changing other providers: its profile owns 32-row candidate overfetch, 20 structured details, canonical sofa-bed query normalization, and deterministic title-family/attribute completion. Live hydration uses eight ordered workers with one retry and parses the visible main price when configurable-product JSON-LD omits `offers.price`; this restored Mobel CA60511/GA76681. Fresh searches cannot mix prior-turn cards. Live checks: 20/20 green dining chairs, and exactly all 9 straight sofa-beds (no normal sofa or corner sofa). See commerce and commerce-provider-profiles.
- 2026-07-17 DECISION — Documented the provider-adoption boundary: stale-card prevention, early product-card streaming, preview/live `searchCatalog` parity, and missing-data safety are universal; query normalization, hydration overfetch, larger detail budgets, structured display completion, custom matchers, and HTML parsing remain explicit provider opt-ins. Keep Verskis at 20 cards; use pagination rather than unbounded categories because live hydration/model/UI cost dominates the cheap DB row fetch. See commerce-provider-profiles.
- 2026-07-17 FIX — Restored the production Edge build for `/api/llm/[publicKey]`. Prompt imports had traversed the aggregate provider registry into Verskis catalog hashing (`node:crypto`), and the existing aggregate commerce import also exposed SSRF DNS/network native modules. Split prompt-only provider metadata into `provider-profiles/guidance.ts` and pure commerce gates into `commerce/capabilities.ts`; server registries re-export them for compatibility. `npm run build` now completes page-data collection without native Node Edge warnings. See commerce-provider-profiles and gotchas.
- 2026-07-17 FIX — Kept the asynchronously populated per-language voice Select controlled for its full lifetime. Its empty value is now Base UI's supported `null` sentinel instead of `undefined`, preventing the enable-voice flow from switching uncontrolled → controlled when the first curated voice is auto-selected. Added a regression test for the exact toggle/fetch/selection sequence. See voice.
- 2026-07-17 FEAT/FIX — Brought ElevenLabs product recommendations to text-chat parity: voice search still uses shared semantic `searchCatalog`, but now returns candidates + provider-budgeted structured facts without showing them; a new `display_products` client tool renders only latest-search ids the voice model verifies against every hard constraint. Voice tool/prompt instructions preserve combined dimensions/color/material/etc., reuse provider-specific query/display guidance, and force agent re-sync with hash `v29`. Lithuanian/English spoken measurements normalize to digit transcripts and centimetre catalog queries (`du metrai ant metro aštuoniasdešimt` → `2 m × 1,8 m` on screen, `200 cm × 180 cm` for search). See voice and commerce-provider-profiles.
- 2026-07-17 FIX — Restored voice-call startup after ElevenLabs rejected the new `display_products` client tool's array parameter schema. Selected product ids now cross the scalar client-tool boundary as JSON text, with backward-compatible parsing and latest-search validation in the browser. Voice-token routes now log bounded upstream validation details instead of hiding every sync failure behind an unexplained 502. See voice and gotchas.
